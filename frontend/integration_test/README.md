# Integration Tests (E2E) для Service Platform

Этот директорий содержит end-to-end (E2E) интеграционные тесты для проверки критических пользовательских сценариев.

## 📋 Список тестов

### 1. `auth_flow_test.dart` - Тесты авторизации и регистрации
**Покрытие:**
- ✅ Успешная регистрация нового пользователя
- ✅ Успешный логин с валидными credentials
- ✅ Неудачный логин с неверными credentials
- ✅ Валидация формы регистрации
- ✅ Проверка невалидного email
- ⏸️ Token refresh после истечения срока (skip)

### 2. `review_mandatory_test.dart` - Тесты обязательных отзывов
**Покрытие:**
- ✅ Блокировка создания записи при наличии незавершенных отзывов
- ✅ Отображение grace period после 3 напоминаний
- ✅ Мастер не может завершить запись без выставления отзыва
- ✅ Создание отзыва из диалога незавершенных записей

**Критически важно:** Эти тесты проверяют ключевую бизнес-логику grace period.

### 3. `feed_geolocation_test.dart` - Тесты геолокационной фильтрации
**Покрытие:**
- ✅ Загрузка feed с радиусом по умолчанию (20 км)
- ✅ Открытие и закрытие фильтров
- ✅ Изменение радиуса поиска на 10 км
- ✅ Переключение радиуса на "Вся страна"
- ✅ Фильтрация по категориям
- ✅ Проверка что посты только в заданном радиусе
- ✅ Запрос разрешения на геолокацию
- ✅ Сброс фильтров к значениям по умолчанию

### 4. `booking_flow_test.dart` - Тесты потока бронирования
**Покрытие:**
- ✅ Полный поток создания записи клиентом
- ✅ Мастер подтверждает запись
- ⏸️ Push-уведомление о подтверждении записи (skip)
- ✅ Мастер завершает запись с обязательным отзывом
- ✅ Клиент оставляет обязательный отзыв после завершения
- ✅ Отмена записи клиентом
- ✅ Отклонение записи мастером
- ✅ Проверка статусов записи в хронологии

### 5. `review_reminders_test.dart` - Тесты системы напоминаний об отзывах
**Покрытие:**
- ✅ Отображение диалога с неотзывленными бронированиями
- ✅ Использование grace period (бесплатный пропуск первый раз)
- ✅ Обычный skip увеличивает счетчик напоминаний
- ✅ Кнопка "Пропустить" доступна только после 3+ напоминаний
- ✅ Навигация к экрану оставления отзывов
- ✅ Отображение информации о grace period в диалоге
- ✅ Цветовая индикация badge счетчика напоминаний
- ✅ Обработка ошибок при API запросах skip

**Критически важно:** Эти тесты проверяют новую систему review reminders с grace period механизмом.

---

## 🚀 Запуск тестов

### Предварительные требования:

1. **Backend должен быть запущен** на `http://localhost:3000`
2. **База данных** должна быть в актуальном состоянии с тестовыми данными
3. **Эмулятор или устройство** должны быть запущены

### Установка зависимостей:

```bash
cd frontend
flutter pub get
```

### Запуск всех E2E тестов:

```bash
# На эмуляторе/устройстве Android
flutter test integration_test/

# На эмуляторе iOS
flutter test integration_test/ -d 'iPhone Simulator'

# На конкретном устройстве
flutter test integration_test/ -d <device_id>
```

### Запуск отдельного теста:

```bash
# Тесты авторизации
flutter test integration_test/auth_flow_test.dart

# Тесты обязательных отзывов
flutter test integration_test/review_mandatory_test.dart

# Тесты геолокации
flutter test integration_test/feed_geolocation_test.dart

# Тесты бронирования
flutter test integration_test/booking_flow_test.dart

# Тесты review reminders
flutter test integration_test/review_reminders_test.dart
```

### Запуск с подробным выводом:

```bash
flutter test integration_test/ --verbose
```

---

## 🔧 Настройка тестовой среды

### 1. Backend конфигурация

Убедитесь, что backend запущен в тестовом режиме:

```bash
cd backend
npm run start:dev
```

### 2. Тестовые данные

Создайте тестовых пользователей в базе данных:

```typescript
// Клиент
{
  email: 'client@example.com',
  password: 'password123',
  role: 'client'
}

// Мастер
{
  email: 'master@example.com',
  password: 'password123',
  role: 'master',
  isMaster: true
}

// Тестовый пользователь
{
  email: 'test@example.com',
  password: 'password123'
}
```

### 3. Эмулятор

Убедитесь, что эмулятор имеет доступ к localhost backend:

**Android:**
Backend должен быть доступен по `http://10.0.2.2:3000`

**iOS:**
Backend должен быть доступен по `http://localhost:3000`

---

## 📊 Интерпретация результатов

### Успешный прогон:

```
00:01 +1: auth_flow_test.dart: Успешная регистрация нового пользователя
00:03 +2: auth_flow_test.dart: Успешный логин с валидными credentials
00:05 +3: auth_flow_test.dart: Неудачный логин с неверными credentials
...
All tests passed!
```

### Неудачный тест:

```
00:02 +1 -1: review_mandatory_test.dart: Блокировка создания записи
  Expected: findsOneWidget
    Actual: _TextFinder:<zero widgets with text "Оставьте отзывы">
```

**Возможные причины:**
1. Backend не вернул ошибку `UNREVIEWED_BOOKINGS_EXIST`
2. Диалог не отображается из-за бага в UI
3. Нет незавершенных записей в тестовой БД

---

## 🐛 Отладка тестов

### Включение скриншотов при ошибках:

```dart
testWidgets('test name', (WidgetTester tester) async {
  try {
    // test code
  } catch (e) {
    await tester.takeScreenshot('error_screenshot');
    rethrow;
  }
});
```

### Замедление выполнения для наблюдения:

```dart
await tester.pumpAndSettle(const Duration(seconds: 2)); // Задержка 2 секунды
```

### Логирование состояния:

```dart
print('Current route: ${GoRouter.of(context).location}');
print('Widgets found: ${find.byType(SomeWidget).evaluate().length}');
```

---

## ⚠️ Важные замечания

### 1. Зависимость от состояния БД
Тесты могут зависеть от существующих данных в БД. Рекомендуется:
- Использовать тестовую БД
- Очищать данные перед каждым прогоном
- Создавать тестовые данные в setUp()

### 2. Таймауты
Некоторые операции (API запросы, навигация) могут занимать время:
```dart
await tester.pumpAndSettle(const Duration(seconds: 5)); // Ждем до 5 секунд
```

### 3. Асинхронность
WebSocket события и real-time обновления требуют дополнительного времени:
```dart
await Future.delayed(const Duration(seconds: 2));
await tester.pumpAndSettle();
```

### 4. Ключи виджетов
Многие тесты используют `Key` для идентификации виджетов:
```dart
find.byKey(const Key('login_email_field'))
```

Убедитесь, что эти ключи присутствуют в коде приложения.

---

## 📈 CI/CD Интеграция

### GitHub Actions пример:

```yaml
name: E2E Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup Flutter
        uses: subosito/flutter-action@v2

      - name: Start Backend
        run: |
          cd backend
          npm install
          npm run start:test &

      - name: Run E2E Tests
        run: |
          cd frontend
          flutter pub get
          flutter test integration_test/
```

---

## 🎯 Добавление новых тестов

### Шаблон нового теста:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:integration_test/integration_test.dart';
import 'package:service_platform/main.dart' as app;

void main() {
  IntegrationTestWidgetsFlutterBinding.ensureInitialized();

  group('New Feature E2E Tests', () {
    testWidgets('Test scenario description', (WidgetTester tester) async {
      app.main();
      await tester.pumpAndSettle();

      // Test steps
      // 1. Setup
      // 2. Execute
      // 3. Assert

      expect(find.text('Expected'), findsOneWidget);
    });
  });
}
```

---

## 📚 Полезные ресурсы

- [Flutter Integration Testing](https://docs.flutter.dev/testing/integration-tests)
- [WidgetTester API](https://api.flutter.dev/flutter/flutter_test/WidgetTester-class.html)
- [Finder API](https://api.flutter.dev/flutter/flutter_test/CommonFinders-class.html)

---

**Последнее обновление:** 2026-01-23
**Статус:** ✅ Все критические сценарии покрыты
**Покрытие:** 5 тестовых файлов, 34+ тестовых сценариев

## Unit Tests

Помимо E2E тестов, проект также включает unit тесты для критических компонентов:

### `test/repositories/review_reminders_repository_test.dart`
**Покрытие:**
- ✅ Получение списка неотзывленных бронирований
- ✅ Пустой список если нет неотзывленных бронирований
- ✅ Обработка ошибок API
- ✅ Парсинг множественных бронирований
- ✅ Skip напоминания с grace period
- ✅ Skip напоминания без grace period
- ✅ Значение isGracePeriod по умолчанию
- ✅ Ошибка 400 (grace period уже использован)
- ✅ Ошибка 403 (нет доступа)
- ✅ Ошибка 404 (бронирование не найдено)

**Запуск unit тестов:**
```bash
cd frontend
flutter test test/repositories/review_reminders_repository_test.dart
```

**Генерация моков:**
```bash
cd frontend
flutter pub run build_runner build
```
