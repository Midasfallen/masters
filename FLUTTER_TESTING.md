# 🧪 Тестирование Flutter приложения локально

## 📱 Полная инструкция по запуску и тестированию

Этот гайд описывает процесс локального тестирования Flutter приложения Service Platform.

---

## ✅ Что уже сделано

- ✅ **11 экранов реализовано** (Splash, Onboarding, Login, Register, Home, Search, и др.)
- ✅ **Material Design 3** с полным соответствием Brand Book (98%)
- ✅ **Исправлены ошибки компиляции** (CardTheme, DialogTheme, типизация)
- ✅ **Готово к запуску** на Android/iOS/Web эмуляторах

---

## 🔄 Шаг 1: Обновление проекта с GitHub

### Windows
```bash
# Перейти в директорию проекта
cd C:\masters\masters

# Отменить незавершенный merge (если есть)
git merge --abort

# Получить последние изменения
git fetch origin
git checkout claude/review-project-docs-FcvZC
git reset --hard origin/claude/review-project-docs-FcvZC

# Или просто pull (если нет конфликтов)
git pull origin claude/review-project-docs-FcvZC --no-edit
```

### Linux/macOS
```bash
# Перейти в директорию проекта
cd ~/masters

# Получить последние изменения
git pull origin claude/review-project-docs-FcvZC
```

---

## ✅ Шаг 2: Проверка Flutter установки

```bash
# Проверить версию и состояние Flutter
flutter doctor -v

# Ожидаемый результат:
# ✓ Flutter (Channel stable)
# ✓ Android toolchain
# ✓ Chrome (для web development)
# ! Xcode (только для macOS/iOS)
```

### Если есть проблемы

**Android toolchain отсутствует:**
```bash
# Установить Android Studio
# https://developer.android.com/studio

# После установки выполнить
flutter doctor --android-licenses
```

**Chrome не найден (для Web):**
```bash
# Установить Google Chrome
# https://www.google.com/chrome/
```

---

## 🏗️ Шаг 3: Создание платформенных файлов

**ВАЖНО:** Проект не содержит папок `android/`, `ios/`, поэтому их нужно создать.

```bash
# Перейти в Flutter проект
cd frontend

# Создать платформенные файлы для Android, iOS, Web
flutter create .

# Это создаст:
# - android/ (конфигурация для Android)
# - ios/ (конфигурация для iOS)
# - web/ (конфигурация для Web)
```

**Альтернатива (только нужные платформы):**
```bash
# Только Android и Web
flutter create --platforms android,web .
```

---

## 📦 Шаг 4: Установка зависимостей

```bash
# Убедитесь, что вы в директории frontend
cd frontend

# Очистить кэш (рекомендуется после git pull)
flutter clean

# Установить зависимости
flutter pub get

# Ожидаемый результат:
# Running "flutter pub get" in frontend...
# Got dependencies!
```

### Если возникли ошибки

**Ошибка версии SDK:**
```bash
# Обновить Flutter до последней версии
flutter upgrade
flutter pub get
```

**Ошибка кэша:**
```bash
# Полная очистка
flutter clean
rm -rf .dart_tool
rm pubspec.lock
flutter pub get
```

---

## 📱 Шаг 5: Проверка доступных эмуляторов

```bash
# Показать список эмуляторов
flutter emulators

# Пример вывода:
# 2 available emulators:
#
# Pixel_9_Pro_XL       • pixel_9_pro_xl       • Google  • android
# apple_ios_simulator  • iOS Simulator        • Apple   • ios
```

### Если эмуляторов нет

**Android эмулятор:**
```bash
# Создать через Android Studio:
# 1. Открыть Android Studio
# 2. Tools → Device Manager
# 3. Create Device → Pixel 9 Pro XL
# 4. Выбрать system image (API 34+)
# 5. Finish
```

**iOS симулятор (только macOS):**
```bash
# Установить Xcode из App Store
# Симулятор создается автоматически
open -a Simulator
```

**Web (Chrome):**
```bash
# Эмулятор не нужен, Chrome должен быть установлен
# Проверить
flutter devices
# chrome (web) • chrome • web-javascript • Google Chrome
```

---

## 🚀 Шаг 6: Запуск эмулятора

### Android
```bash
# Запустить эмулятор
flutter emulators --launch Pixel_9_Pro_XL

# Подождать 10-15 секунд, пока эмулятор загрузится

# Проверить, что устройство доступно
flutter devices

# Должно показать:
# sdk gphone64 x86 64 (emulator-5554) • emulator-5554 • android-x64 • Android ...
```

### iOS (macOS)
```bash
# Запустить симулятор
open -a Simulator

# Проверить доступные устройства
flutter devices
```

### Web
```bash
# Эмулятор не нужен, просто запустите приложение
flutter run -d chrome
```

---

## ▶️ Шаг 7: Запуск приложения

### Стандартный запуск (Debug mode)

```bash
# Убедитесь, что вы в директории frontend
cd frontend

# Запустить на первом доступном устройстве
flutter run

# Или явно указать устройство
flutter run -d emulator-5554      # Android
flutter run -d chrome              # Web
flutter run -d "iPhone 15"         # iOS (macOS)
```

### Release mode (оптимизированная версия)

```bash
# Запуск в release mode (быстрее, но без hot reload)
flutter run --release
```

### С подробными логами

```bash
# Запуск с verbose логами (для отладки)
flutter run -v
```

---

## 🎯 Быстрый запуск (все шаги одной командой)

### Windows (PowerShell)

```powershell
cd C:\masters\masters
git fetch origin
git checkout claude/review-project-docs-FcvZC
git reset --hard origin/claude/review-project-docs-FcvZC
cd frontend
flutter create .
flutter clean
flutter pub get
flutter emulators --launch Pixel_9_Pro_XL
Start-Sleep -Seconds 15
flutter run
```

### Linux/macOS (Bash)

```bash
cd ~/masters && \
git pull origin claude/review-project-docs-FcvZC && \
cd frontend && \
flutter create . && \
flutter clean && \
flutter pub get && \
flutter emulators --launch Pixel_9_Pro_XL && \
sleep 15 && \
flutter run
```

### Web (любая ОС)

```bash
cd frontend && \
flutter clean && \
flutter pub get && \
flutter run -d chrome
```

---

## 🧪 Дополнительные команды для тестирования

### Горячая перезагрузка (во время работы приложения)

```bash
# В терминале, где запущен flutter run:
r       # Hot reload (обновить изменения без перезапуска)
R       # Hot restart (полный перезапуск)
q       # Quit (выход)
```

### Запуск тестов

```bash
# Запустить все тесты
flutter test

# Запустить с coverage
flutter test --coverage

# Запустить конкретный тест
flutter test test/widget_test.dart
```

### Анализ кода

```bash
# Проверить код на ошибки
flutter analyze

# Форматирование кода
flutter format lib/

# Проверить устаревшие зависимости
flutter pub outdated

# Обновить зависимости
flutter pub upgrade
```

### Показать все устройства

```bash
# Показать все доступные устройства
flutter devices

# Пример вывода:
# 3 connected devices:
# sdk gphone64 x86 64 (mobile) • emulator-5554 • android-x64    • Android 14 (API 34)
# Chrome (web)                  • chrome        • web-javascript • Google Chrome 120
# Edge (web)                    • edge          • web-javascript • Microsoft Edge 120
```

---

## 🐛 Решение типичных проблем

### Проблема: "No connected devices"

**Решение:**
```bash
# 1. Проверить, что эмулятор запущен
flutter emulators --launch Pixel_9_Pro_XL
sleep 15

# 2. Проверить устройства
flutter devices

# 3. Перезапустить ADB (Android)
adb kill-server
adb start-server

# 4. Попробовать снова
flutter run
```

### Проблема: "Gradle build failed"

**Решение:**
```bash
# Windows
cd android
.\gradlew clean
cd ..
flutter clean
flutter pub get
flutter run

# Linux/macOS
cd android
./gradlew clean
cd ..
flutter clean
flutter pub get
flutter run
```

### Проблема: "CocoaPods not installed" (iOS, macOS)

**Решение:**
```bash
# Установить CocoaPods
sudo gem install cocoapods

# Установить зависимости
cd ios
pod install
cd ..

# Запустить приложение
flutter run
```

### Проблема: "Build failed" после git pull

**Решение:**
```bash
# Полная очистка
flutter clean
rm -rf .dart_tool
rm -rf build
rm pubspec.lock

# Переустановка
flutter pub get
flutter run
```

### Проблема: Ошибки типов (CardTheme, DialogTheme)

**Статус:** ✅ **Уже исправлено** (коммит `c2e2cc2`)

Если всё равно видите ошибки:
```bash
# Убедитесь, что у вас последняя версия с GitHub
git fetch origin
git reset --hard origin/claude/review-project-docs-FcvZC

# Полная очистка
flutter clean
flutter pub get
flutter run
```

### Проблема: Эмулятор не запускается

**Android:**
```bash
# Проверить доступную память
# Android эмулятор требует минимум 8GB RAM

# Проверить виртуализацию
# Windows: включить Hyper-V или HAXM
# Linux: включить KVM

# Создать эмулятор с меньшими параметрами
# В Android Studio: Tools → Device Manager
# Выбрать Pixel 6 (меньше RAM) вместо Pixel 9 Pro XL
```

**iOS (macOS):**
```bash
# Переустановить Xcode Command Line Tools
xcode-select --install

# Открыть Xcode и обновить компоненты
open -a Xcode
```

---

## 📊 Статус проекта

| Компонент | Статус | Прогресс |
|-----------|--------|----------|
| **Документация** | ✅ Готово | 100% |
| **Brand Book** | ✅ Готово | 100% |
| **Flutter прототип** | ✅ Готово | 100% |
| **Исправления ошибок** | ✅ Готово | 100% |
| **Backend** | ⏳ В планах | 0% |
| **Интеграция** | ⏳ В планах | 0% |

---

## 🎨 Реализованные экраны

### ✅ Авторизация (4 экрана)
- **Splash Screen** - с градиентом primaryGradient
- **Onboarding Screen** - свайпер с 3 слайдами
- **Role Selection** - выбор роли (Клиент/Мастер)
- **Login/Register** - формы входа и регистрации

### ✅ Основное приложение (7 экранов)
- **Home Screen** - главная с категориями и мастерами
- **Search Screen** - поиск мастеров с фильтрами
- **Favorites Screen** - избранные мастера
- **Bookings Screen** - мои записи (предстоящие/история)
- **Profile Screen** - профиль пользователя
- **Master Profile** - детальная карточка мастера
- **Booking Flow** - процесс бронирования

---

## 📚 Дополнительная документация

- **[Brand Book](docs/design/BrandBook.md)** - фирменный стиль и дизайн-система
- **[UX/UI Guide](docs/design/UXUI-Guide.md)** - описание всех экранов
- **[API Specification](docs/technical/API.md)** - API endpoints (для будущего backend)
- **[Tech Spec](docs/technical/TechSpec.md)** - техническая спецификация
- **[Product Backlog](docs/analysis/Backlog.md)** - план разработки (120+ задач)

---

## ✅ Итоговый чеклист запуска

- [ ] 1. `git pull origin claude/review-project-docs-FcvZC` - обновить код
- [ ] 2. `cd frontend` - перейти в Flutter проект
- [ ] 3. `flutter create .` - создать платформенные файлы (первый раз)
- [ ] 4. `flutter doctor` - проверить Flutter
- [ ] 5. `flutter clean` - очистить кэш
- [ ] 6. `flutter pub get` - установить зависимости
- [ ] 7. `flutter emulators --launch Pixel_9_Pro_XL` - запустить эмулятор
- [ ] 8. Подождать 15 секунд пока эмулятор загрузится
- [ ] 9. `flutter run` - запустить приложение
- [ ] 10. Протестировать все экраны и функциональность

---

## 🎯 Что тестировать

### Навигация
- [ ] Splash → Onboarding → Role Selection → Login
- [ ] Переход между табами (Home, Search, Favorites, Bookings, Profile)
- [ ] Возврат назад (кнопка Back)

### UI/UX
- [ ] Соответствие Brand Book (цвета, типографика, градиенты)
- [ ] Адаптивность на разных размерах экрана
- [ ] Плавность анимаций и переходов
- [ ] Читаемость текста

### Функциональность
- [ ] Поиск мастеров с фильтрами
- [ ] Добавление в избранное
- [ ] Создание и отмена записей
- [ ] Переключение темы (Light/Dark)
- [ ] Изменение языка

### Производительность
- [ ] Скорость загрузки экранов
- [ ] Плавность скроллинга
- [ ] Отзывчивость кнопок

---

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте версию Flutter:** `flutter --version` (должна быть 3.2.0+)
2. **Очистите кэш:** `flutter clean && flutter pub get`
3. **Проверьте логи:** `flutter run -v`
4. **Создайте issue:** https://github.com/Midasfallen/masters/issues

---

**Версия:** 1.0
**Дата:** 24 декабря 2025
**Статус:** ✅ Готово к тестированию
**Последнее обновление:** Исправлены ошибки компиляции (CardTheme, DialogTheme, типизация)
