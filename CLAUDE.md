# CLAUDE.MD — Правила разработки Service Platform

Строго следовать C:\Users\ravin\.claude\settings.json

## 🎯 Архитектура (Монорепозиторий)
- **Frontend:** `./frontend` (Flutter + Riverpod).
- **Backend:** `./backend` (NestJS + TypeORM) — **Запуск локально для Hot Reload**.
- **Infrastructure:** Docker (PostgreSQL + PostGIS, Minio, Redis) — **Запуск в контейнерах**.

## ⚠️ СТАНДАРТ ИМЕНОВАНИЯ И СВЯЗЕЙ
### Маппинг данных:
- **JSON (API) & Database:** `snake_case` (пример: `first_name`, `author_id`).
- **Dart & TypeScript code:** `camelCase` (пример: `firstName`, `authorId`).
- **Исключение:** Поле `parentId` в таблице `categories` (сохранять как есть для совместимости).

### Важное по БД (Фильтры):
- **ID Мастера:** `master_id` часто ссылается напрямую на `users.id`.
- **Поиск профиля:** Бэкенд ОБЯЗАН искать профиль мастера как по `master_profile.id`, так и по `user_id`.
- **Связи Постов:** Пост связан с услугами через таблицу `post_services` (многие-ко-многим).

## 🚀 РЕЖИМ РАЗРАБОТКИ (ГИБРИДНЫЙ)
- **Backend (Priority):** Запускать через `npm run start:dev` прямо на хосте.
- **Инфраструктура:** Всегда поднята в Docker (`service_postgres`, `service_minio`, и т.д.).
- **Подключение:** Локальный бэкенд подключается к БД через `localhost:5433`.
- **ПРОВЕРКА КОНФЛИКТОВ:** Перед запуском бэка локально ОБЯЗАТЕЛЬНО остановить контейнер: `docker stop service_backend`. Два одновременно запущенных бэкенда (в докере и локально) ГАРАНТИРУЮТ путаницу и ошибки портов.

## 🐳 Docker & Runtime
- **Infrastructure Only:** В Docker держим только сервисы (БД, Redis, Minio). 
- **Backend в Docker:** Использовать только для финального тестирования. Если используется контейнер бэкенда, ВСЕГДА пересобирать: `npm run build` -> `docker-compose up --build backend`.

## 🛠 Команды проекта
### Backend (Локально)
- Запуск: `npm run start:dev`
- Сборка: `npm run build`
- Миграции: `npm run migration:run`

### Frontend
- **Обновление зависимостей:** Перед большими фичами делать `flutter pub upgrade`.
- **Кодогенерация:** `flutter pub run build_runner build --delete-conflicting-outputs`.
- **Запуск Web:** `flutter run -d chrome` (в Flutter 3.35+ рендерер выбирается автоматически, по умолчанию используется CanvasKit)

## 📋 Технические правила
- **Геопозиция:** Использовать PostGIS (`ST_DWithin`).
- **Фильтрация:** - По категориям: оператор `&&` для массивов UUID.
  - По услугам: через `post_services` и текстовый поиск по `custom_service_name`.
- **State Management:** Riverpod 2.x. Делать `ref.invalidate` при навигации, чтобы сбросить кэш ошибок.

## 📦 MinIO Configuration
- **MINIO_PUBLIC_URL**: Для локальной разработки всегда должен быть `http://localhost:9000`
- **Проверка**: Убедиться, что бэкенд не возвращает внутренние Docker-имена (`service_minio`) в API ответах
- **CORS**: Настроен автоматически через `docker-compose.yml` при создании бакетов
- **Media Links**: Бэкенд генерирует публичные URL на основе `MINIO_PUBLIC_URL`, а не внутреннего endpoint

## 🧪 Тестирование и Данные
- **Среда:** Локальный бэкенд + Docker инфраструктура + Chrome Web.
- **Seeds:** - БД: `backend/src/database/seeds/test-data.seed.ts`
  - Minio: `backend/src/database/seeds/upload-local-images.ts`
- **Проверка:** Перед дебагом кода проверить связь в БД через SQL-запрос в DBeaver.

## 🆘 Protocol "Senior Brother" (Эскалация)
1. **Триггер**: Ошибка не решается за **3 итерации**.
2. **Действие**: Вывод сообщения "🆘 **ОШИБКА ЦИКЛА: ТРЕБУЕТСЯ СТАРШАЯ МОДЕЛЬ**".
3. **Остановка**: Ждать ручного ввода.