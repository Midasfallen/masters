# 🚀 Инструкция по запуску

## ✅ Предварительная проверка

### Docker контейнеры (уже запущены ✅)
Все необходимые контейнеры работают:
- ✅ `service_postgres` - PostgreSQL (порт 5433)
- ✅ `service_minio` - MinIO (порт 9000)
- ✅ `service_redis` - Redis (порт 6379)
- ✅ `service_meilisearch` - Meilisearch (порт 7700)
- ✅ `service_adminer` - Adminer (порт 8080)
- ✅ `service_mailhog` - MailHog (порт 8025)

**Если контейнеры не запущены:**
```bash
docker-compose up -d
```

### ⚠️ ВАЖНО: Остановить контейнер бэкенда
Перед запуском локального бэкенда ОБЯЗАТЕЛЬНО остановите контейнер:
```bash
docker stop service_backend
```

---

## 📋 Команды для запуска

### 1. Backend (в первом терминале)
```bash
cd backend
npm run start:dev
```

**Ожидаемый результат:**
- Сервер запускается на `http://localhost:3000`
- В консоли видны логи подключения к БД
- Swagger UI: `http://localhost:3000/api/v2/docs`

**Если возникли ошибки:**
- Проверьте, что контейнер бэкенда остановлен: `docker ps | grep service_backend`
- Проверьте подключение к БД: `docker logs service_postgres --tail 20`

### 2. Frontend (во втором терминале)
```bash
cd frontend
flutter run -d chrome
```

**Ожидаемый результат:**
- Приложение открывается в Chrome
- DevTools доступны (F12)
- Hot Reload работает

**Если возникли ошибки:**
- Проверьте зависимости: `flutter pub get`
- Проверьте версию Flutter: `flutter --version` (должна быть 3.35+)

---

## 🔍 Проверка работоспособности

### Backend
1. Откройте Swagger UI: http://localhost:3000/api/v2/docs
2. Проверьте Health Check: http://localhost:3000/health
3. Попробуйте авторизоваться через `/auth/login`

### Frontend
1. Откройте DevTools (F12) → вкладка **Network**
2. Авторизуйтесь в приложении
3. Проверьте, что запросы идут на `http://localhost:3000/api/v2`

---

## 📝 Что было исправлено

### ✅ Создание поста
- Исправлена структура `CreatePostRequest` для соответствия DTO бэкенда
- Добавлено обязательное поле `type: PostType`
- Исправлен формат медиафайлов (`media` вместо `media_urls`)
- Убрано поле `tags` (бэкенд не принимает)

### ✅ Runtime ошибки
- Исправлен TypeError: null is not a subtype of type 'String'
- Добавлены безопасные проверки в `CommentModel.fromJson`
- Исправлена обработка `XFile.name` на Web

### ✅ Тесты
- Обновлены тесты для использования нового формата `CreatePostRequest`
- Все тесты проходят без ошибок

---

## 🐛 Типичные проблемы

### Backend не запускается
**Ошибка:** `Port 3000 is already in use`
```bash
# Решение 1: Остановить контейнер бэкенда
docker stop service_backend

# Решение 2: Найти процесс на порту 3000
netstat -ano | findstr :3000
# Затем завершить процесс по PID
```

**Ошибка:** `ECONNREFUSED` или `relation does not exist`
```bash
# Проверить статус PostgreSQL
docker logs service_postgres --tail 20

# Применить миграции (если нужно)
cd backend
npm run migration:run
```

### Frontend не подключается к Backend
**Ошибка:** CORS ошибки в консоли
- Убедитесь, что бэкенд запущен: `curl http://localhost:3000/health`
- Проверьте конфигурацию в `frontend/lib/core/config/app_config.dart`

**Ошибка:** `Failed to load resource: net::ERR_CONNECTION_REFUSED`
- Убедитесь, что бэкенд запущен на порту 3000
- Проверьте URL в `app_config.dart` (должен быть `http://localhost:3000`)

---

## 📚 Дополнительная документация

- `docs/QUICK_START.md` - детальная инструкция по запуску
- `docs/TESTING_CHECKLIST.md` - чек-лист для тестирования
- `docs/CREATE_POST_FIX.md` - исправление создания поста
- `docs/RUNTIME_ERRORS_FIX.md` - исправление runtime ошибок
- `docs/BACKEND_FRONTEND_SYNC.md` - синхронизация фронтенда и бэкенда

---

## ✨ Готово к тестированию!

После запуска обоих сервисов можно тестировать:
1. ✅ Создание поста (исправлено)
2. ✅ Загрузка медиафайлов
3. ✅ Лайки и комментарии
4. ✅ Аватары с fallback
5. ✅ Поделиться постом

**Удачи! 🚀**
