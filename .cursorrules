# Cursor Rules для Service Platform
## Behavioral Operating System (BOS) - Система оркестрации задач

---

## 🎯 CORE RULES - ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА ОРКЕСТРАЦИИ

### 1. GATHER FULL CONTEXT FIRST (MANDATORY) ⚠️

**Перед выполнением или делегированием ЛЮБОЙ задачи:**

1. **Прочитай существующий код:**
   - Все связанные файлы (контроллеры, сервисы, модели, DTO)
   - Поиск по кодовой базе похожих паттернов
   - Проверка зависимостей и точек интеграции

2. **Изучи документацию:**
   - Технические спецификации (`docs/technical/`)
   - API документация (`docs/api/`)
   - Архитектурные решения (`docs/architecture/`)
   - Недавние коммиты в связанных областях

3. **Проверь контекст проекта:**
   - Стандарты именования (snake_case для БД/API, camelCase для кода)
   - Паттерны проекта (NestJS модули, Riverpod провайдеры)
   - Существующие утилиты и сервисы

**НИКОГДА не выполняй задачи вслепую без полного контекста.**

---

### 2. DELEGATE TO SUBAGENTS (Специализированные задачи)

**Когда делегировать:**
- Сложные задачи (> 50 строк кода)
- Задачи, требующие изменений в нескольких файлах
- Задачи с зависимостями между модулями
- Задачи, требующие тестирования

**Перед делегированием:**
1. **Собери полный контекст** (см. правило 1)
2. **Укажи точные требования:**
   - Конкретные файлы и функции
   - Ожидаемый результат
   - Критерии валидации
   - Примеры из существующего кода

**После делегирования (КРИТИЧНО):**
1. **ВСЕГДА проверяй результаты:**
   - Прочитай измененные файлы
   - Запусти type-check (`npm run build` или `flutter analyze`)
   - Проверь соответствие стандартам проекта
   - Убедись, что все зависимости обновлены

2. **Если результат неверен:**
   - НЕ исправляй вручную
   - Переделегируй с исправлениями и описанием ошибок
   - Укажи конкретные места, где нужны изменения

**Типы субагентов (специализации):**
- **Backend Agent**: NestJS, TypeORM, PostgreSQL, валидация DTO
- **Frontend Agent**: Flutter, Riverpod, UI компоненты, навигация
- **Testing Agent**: Unit тесты, E2E тесты, тестовые данные
- **Database Agent**: Миграции, запросы, оптимизация индексов
- **Integration Agent**: API интеграция, WebSocket, внешние сервисы
- **Documentation Agent**: Обновление документации, API docs, README

---

### 3. EXECUTE DIRECTLY (Минимальные задачи только)

**Прямое выполнение ТОЛЬКО для:**
- Установка одной зависимости (`npm install package`)
- Исправление опечаток (1-2 строки)
- Простые импорты
- Минимальные изменения конфигурации
- Форматирование кода

**Всё остальное: делегируй специализированным агентам.**

---

### 4. CONTEXT ISOLATION (Изоляция контекста)

**Проблема:** Долгая работа в одной сессии переполняет контекстный бюджет.

**Решение:**
1. **Разбивай большие задачи на подзадачи**
2. **Каждая подзадача = отдельный контекст**
3. **Используй Memory MCP** для сохранения важной информации между сессиями
4. **После завершения задачи:** очищай контекст, оставляя только результат

**Пример:**
```
❌ Плохо: "Сделай всю систему аутентификации" (50K токенов контекста)
✅ Хорошо: 
  - Подзадача 1: "Создай AuthProvider" (10K токенов)
  - Подзадача 2: "Добавь методы login/logout" (10K токенов)
  - Подзадача 3: "Интегрируй с API" (10K токенов)
```

---

## 📋 PLAN MODE RULES - ПРАВИЛА АВТОМАТИЧЕСКОГО ПЛАНИРОВАНИЯ

### 1. АВТОМАТИЧЕСКОЕ ОПРЕДЕЛЕНИЕ СУБАГЕНТОВ

**При создании плана в режиме планирования:**

1. **Автоматически анализируй задачу:**
   - Определяй тип задачи по ключевым словам и контексту
   - Выявляй необходимых субагентов для выполнения
   - Определяй критичные операции, требующие MCP

2. **Алгоритм определения типа агента:**
   - **Backend Agent**: "API", "endpoint", "controller", "service", "NestJS", "TypeORM", "модуль", "DTO"
   - **Frontend Agent**: "экран", "виджет", "Flutter", "Riverpod", "UI", "навигация", "провайдер"
   - **Database Agent**: "миграция", "таблица", "SQL", "PostgreSQL", "индекс", "constraint", "seed"
   - **Testing Agent**: "тест", "покрытие", "unit", "e2e", "проверка", "валидация"
   - **Integration Agent**: "интеграция", "WebSocket", "подключение", "внешний сервис", "MinIO", "Redis"
   - **Documentation Agent**: "документация", "README", "Swagger", "обновить docs", "API docs"

3. **Структурирование плана:**
   - Каждый шаг плана должен быть явно делегирован соответствующему агенту
   - Формат: `### Шаг N: [Agent Type] [Название подзадачи]`
   - Каждый шаг должен содержать: КОНТЕКСТ, ТРЕБОВАНИЯ, MCP ИСПОЛЬЗОВАНИЕ (если применимо), КРИТЕРИИ ВАЛИДАЦИИ

### 2. ИНТЕГРАЦИЯ MCP В ПЛАНЫ

**Обязательное использование MCP для критичных операций:**

1. **Postgres MCP** - ВСЕГДА для операций с БД:
   - Проверка структуры БД перед созданием миграций
   - Проверка данных после операций
   - Валидация схемы перед изменениями
   - Формат: `MCP ИСПОЛЬЗОВАНИЕ: Postgres MCP - проверка структуры таблицы users перед миграцией`

2. **Git MCP** - ВСЕГДА для операций с Git:
   - Проверка статуса перед коммитом
   - Создание коммитов (после валидации)
   - Просмотр истории изменений
   - Формат: `MCP ИСПОЛЬЗОВАНИЕ: Git MCP - проверка статуса перед коммитом изменений`

3. **Docker MCP** - для операций с контейнерами:
   - Проверка статуса контейнеров перед операциями
   - Просмотр логов для диагностики
   - Формат: `MCP ИСПОЛЬЗОВАНИЕ: Docker MCP - проверка статуса контейнеров перед запуском`

4. **Redis MCP** - для операций с кешем:
   - Проверка кеша перед операциями
   - Очистка данных при необходимости
   - Формат: `MCP ИСПОЛЬЗОВАНИЕ: Redis MCP - очистка кеша после изменения данных`

5. **Memory MCP** - для сохранения контекста:
   - Сохранение важной информации между сессиями
   - Сохранение архитектурных решений
   - Сохранение результатов анализа
   - Формат: `MCP ИСПОЛЬЗОВАНИЕ: Memory MCP - сохранение архитектурного решения для будущих задач`

6. **Filesystem MCP** - для анализа файлов:
   - Чтение логов для диагностики
   - Анализ структуры файлов
   - Формат: `MCP ИСПОЛЬЗОВАНИЕ: Filesystem MCP - анализ структуры файлов перед рефакторингом`

7. **GitHub MCP** - для операций с GitHub:
   - Создание issues для отслеживания задач
   - Просмотр PR для понимания контекста
   - Формат: `MCP ИСПОЛЬЗОВАНИЕ: GitHub MCP - создание issue для отслеживания задачи`

**Правила использования MCP:**
- Явно указывай MCP только для критичных операций (БД, Git, Docker)
- Автоматически используй MCP для проверки результатов после выполнения шага
- Используй Memory MCP для сохранения контекста проекта между сессиями

### 3. СТРУКТУРА ПЛАНА С ДЕЛЕГИРОВАНИЕМ

**Обязательная структура каждого плана:**

```markdown
# План: [Название задачи]

## Анализ задачи
- Тип задачи: [Backend/Frontend/Database/etc]
- Необходимые агенты: [список агентов]
- Критичные операции: [список операций с MCP]

## План выполнения

### Шаг 1: [Backend Agent] [Название подзадачи]
КОНТЕКСТ:
- Связанные файлы: [список файлов]
- Похожие паттерны: [ссылки на примеры]
- Документация: [ссылки на docs]
- Зависимости: [список зависимостей]

ТРЕБОВАНИЯ:
- [конкретное требование 1]
- [конкретное требование 2]

MCP ИСПОЛЬЗОВАНИЕ:
- [MCP сервер]: [описание использования]

КРИТЕРИИ ВАЛИДАЦИИ:
- [критерий 1]
- [критерий 2]

### Шаг 2: [Frontend Agent] [Название подзадачи]
...
```

### 4. АВТОМАТИЧЕСКОЕ СОХРАНЕНИЕ КОНТЕКСТА

**Использование Memory MCP:**
- После завершения каждого шага сохраняй важную информацию в Memory MCP
- Сохраняй архитектурные решения для будущих задач
- Сохраняй результаты анализа для предотвращения повторной работы
- Формат: `Memory MCP - сохранение [тип информации]: [описание]`

---

## 🏗️ АРХИТЕКТУРА ПРОЕКТА

### Backend (NestJS + TypeORM)
- **Модульная структура:** Каждый модуль = отдельная папка
- **Naming:** snake_case для БД/API, camelCase для кода
- **Валидация:** class-validator для всех DTO
- **Транзакции:** Всегда для связанных операций

### Frontend (Flutter + Riverpod)
- **Feature-based структура:** Каждая фича = отдельная папка
- **State Management:** Riverpod 2.x, всегда `ref.invalidate()` после мутаций
- **Null-safety:** Используй null-safety, избегай `!` оператора

### Работа с БД
- Всегда используй транзакции для связанных операций
- Проверяй существование записей перед обновлением
- Используй индексы для частых запросов
- PostGIS для геолокации: `ST_DWithin`

### Архитектура проекта
- **Backend:** NestJS + TypeORM, модульная структура
- **Frontend:** Flutter + Riverpod, feature-based структура
- **Naming:** snake_case для БД/API, camelCase для кода
- **State Management:** Riverpod 2.x, всегда `ref.invalidate()` после мутаций

### Стандарты кода
- TypeScript: строгая типизация, избегай `any`
- Dart: используй null-safety, избегай `!` оператора где возможно
- Всегда обрабатывай ошибки явно
- Используй async/await вместо промисов/фьючеров

### Работа с БД
- Всегда используй транзакции для связанных операций
- Проверяй существование записей перед обновлением
- Используй индексы для частых запросов
- PostGIS для геолокации: `ST_DWithin`

### API и DTO
- Всегда валидируй входные данные через class-validator
- Используй DTO для всех запросов/ответов
- Следуй RESTful принципам
- Возвращай понятные сообщения об ошибках

### Тестирование
- Покрывай критичные функции unit-тестами
- Используй E2E тесты для основных сценариев
- Проверяй edge cases (null, empty, invalid data)

### Git и коммиты
- Пиши понятные commit messages на русском
- Группируй связанные изменения в один коммит
- Не коммить временные файлы и логи

## 🚫 Что НЕ делать

- Не создавай дублирующий код
- Не игнорируй ошибки линтера
- Не коммить `.env` файлы или секреты
- Не используй `any` в TypeScript без крайней необходимости
- Не забывай обновлять документацию при изменении API

## ✅ Что делать всегда

- Проверяй линтер перед коммитом
- Тестируй изменения локально
- Обновляй документацию при изменении API
- Используй существующие паттерны проекта
- Следуй принципу DRY (Don't Repeat Yourself)
