# Комплексный план доработки Service Platform

## Дата: 2026-01-22
**Статус:** В процессе реализации

## Прогресс реализации

### ✅ Спринт 1: ЗАВЕРШЕН (Критические исправления)
- ✅ 1.1 Цветовая схема Material 3 - полностью реализована
- ✅ 1.2 Обязательная система отзывов (Backend + Frontend) - полностью реализована
- ✅ 1.3 Геолокационная фильтрация - полностью реализована
- ✅ 1.4 Доработка экрана деталей поста - уже был реализован

### ✅ Спринт 2: 100% ЗАВЕРШЕН (Функциональные доработки)
- ✅ 2.1 Список услуг мастера (Expand/Collapse) - полностью реализован
- ✅ 2.2 Социальные функции (Друзья и подписки) - полностью реализованы
- ✅ 2.3 Интеграционные E2E тесты - полностью реализованы (26+ тестов)
- ✅ 2.4 Удаление TODO + print() - полностью завершено

### 🔄 Текущая задача
Начало Спринта 3 (MEDIUM Priority - UX/UI полировка и дополнительные функции)

## Решения по ключевым вопросам

После согласования с заказчиком приняты следующие решения:

1. **Объем работ:** Реализация всех 4 спринтов последовательно (полная доработка до production-ready)
2. **Google OAuth:** Отложен на пост-MVP фазу
3. **Обязательные отзывы:** Реализация с grace period (3 напоминания) для более мягкого UX
4. **Документация:** Создание TESTING.md и DEPLOYMENT_CHECKLIST.md после Спринта 2

**Общая оценка:** 16-24 дня работы (4 спринта по 3-7 дней)

---

## Резюме проблем

### Текущая ситуация
После анализа проекта выявлены **критические проблемы**:

1. **Ошибки подключения и парсинга данных** (частично исправлены в последнем коммите e98342e)
2. **Несоответствие дизайн-гайду v2** (UXUI-Guide-v2.md) - приложение ~35-40% готово
3. **Цветовая схема не соответствует Material 3** (используется Instagram-стиль вместо #6750A4)
4. **Отсутствие критической бизнес-логики** (обязательные отзывы, геолокация)
5. **Множество незавершенных TODO** в коде
6. **E2E тесты не покрывают реальные сценарии**

### Критичность
**HIGH PRIORITY** - Приложение не готово к production без этих исправлений

---

## Фаза 1: Критические исправления (Backend + Frontend)

### 1.1 Исправление цветовой схемы Material 3
**Файлы:** `frontend/lib/core/theme/app_colors.dart`, `app_theme.dart`

**Проблема:** Используется Instagram gradient (#405DE6, #5851DB, #833AB4)
**Требование из дизайн-гайда:**
- Primary: #6750A4 (Material 3 Purple)
- Secondary: #E91E63 (Pink, для избранного/premium)
- Tertiary: #00BCD4 (Teal, для подтверждений)

**Действия:**
1. Обновить `AppColors.primary/secondary/tertiary` на правильные значения
2. Обновить `app_theme.dart` для использования Material 3 ColorScheme
3. Проверить все виджеты на корректное отображение с новыми цветами
4. Обновить градиенты (если используются)

---

### 1.2 Обязательная система отзывов (Backend + Frontend)
**Критическая бизнес-логика!**

**Требование из дизайн-гайда (с grace period):**
- Клиент получает 3 напоминания об отзывах перед блокировкой
- После 3-го отказа разрешается пропустить отзыв
- Мастер НЕ МОЖЕТ завершить запись без выставления отзыва клиенту

#### Backend изменения:
**Файл:** `backend/src/modules/bookings/bookings.service.ts`

1. Добавить метод `checkUnreviewedBookings(userId: string)`:
   - Проверяет наличие завершенных записей без отзывов
   - Возвращает список незаверенных записей

2. Обновить метод `create()`:
   - Перед созданием записи вызывать `checkUnreviewedBookings()`
   - Если есть незаверенные И пользователь не имеет grace skip:
     - Выбросить `BadRequestException` с кодом `UNREVIEWED_BOOKINGS_EXIST`
     - Вернуть список ID незаверенных записей
   - Grace period: После 3 напоминаний разрешить skip

3. Добавить таблицу `review_reminders`:
   - `user_id`, `booking_id`, `reminder_count` (max 3)
   - После 3 напоминаний установить флаг `grace_skip_allowed`

3. Обновить метод `complete()` (для мастера):
   - Добавить обязательное поле `rating: number` и `review_text?: string`
   - Автоматически создавать отзыв при завершении

**Файл:** `backend/src/modules/reviews/reviews.service.ts`
- Добавить метод `createFromBookingCompletion()` для автоматического создания отзыва

#### Frontend изменения:
**Файлы:**
- `frontend/lib/core/repositories/booking_repository.dart`
- `frontend/lib/features/bookings/screens/bookings_screen.dart`
- `frontend/lib/core/api/api_exceptions.dart`

1. Обновить `BookingRepository.createBooking()`:
   - Обработать ошибку `UNREVIEWED_BOOKINGS_EXIST`
   - Бросать новый тип исключения `UnreviewedBookingsException`

2. Создать экран `UnreviewedBookingsModal`:
   - Показывать список записей, требующих отзыва
   - Кнопки "Оставить отзыв" для каждой записи
   - Блокировать создание новой записи до прохождения всех отзывов

3. Обновить экран завершения записи (мастер):
   - Добавить обязательные поля: Rating (1-5 звезд) и комментарий
   - Кнопка "Завершить" неактивна без рейтинга

**Приоритет:** CRITICAL

---

### 1.3 Геолокационная фильтрация
**Файлы:** Backend + Frontend

#### Backend:
**Файл:** `backend/src/modules/posts/posts.service.ts`

1. Обновить метод `getFeed()`:
   - Добавить параметр `radius?: number` (1, 5, 10, 20, 25, 50 км, или null для "Вся страна")
   - Использовать PostGIS для расчета расстояния:
     ```sql
     ST_Distance_Sphere(
       point(post.location_lng, post.location_lat),
       point(userLng, userLat)
     ) <= radius * 1000
     ```
   - По умолчанию: radius = 20 км

2. Добавить параметр `category?: string` для фильтрации по категориям

**Файл:** `backend/src/modules/users/users.service.ts`
- Обновить метод `updateMe()` для сохранения `last_location_lat/lng` при каждом обновлении локации

#### Frontend:
**Файлы:**
- `frontend/lib/features/feed/screens/feed_screen.dart`
- `frontend/lib/features/feed/widgets/feed_filters_chip.dart` (создать новый)

1. Добавить горизонтальный скролл с чипами над grid:
   ```dart
   [📍 20 км] [📂 Все] [👥 Друзья] [📌 Подписки] [⭐ Избранные]
   ```

2. Создать `FeedFiltersChip` виджет:
   - Поддержка выбора радиуса: 1, 5, 10, 20, 25, 50 км, "Вся страна"
   - Фильтр по категориям
   - Фильтр по друзьям/подпискам

3. Обновить `FeedProvider`:
   - Добавить параметры `radius`, `category`, `friendsOnly`, `subscriptionsOnly`
   - Передавать параметры в API запрос

4. Интегрировать `geolocator` пакет:
   - Запрашивать разрешение на геолокацию при первом запуске
   - Сохранять координаты пользователя
   - Обновлять координаты периодически (каждые 15 минут в фоне)

**Приоритет:** CRITICAL

---

### 1.4 Доработка экрана деталей поста
**Файл:** `frontend/lib/features/feed/screens/post_detail_screen.dart`

**Проблема:** Отсутствует нижняя секция с профилем мастера и CTA-кнопками

**Требование из дизайн-гайда:**
- Правая панель с вертикальными кнопками: ❤️ Лайк, 💬 Комментарии, 📤 Поделиться, ⭐ Избранное
- Нижняя секция:
  - Профиль мастера с кнопкой "Подписаться"
  - Описание поста с упоминаниями (@username) и хештегами (#tag)
  - Отмеченная услуга (если есть)
  - CTA кнопки: "📅 Записаться" и "👤 К мастеру"

**Действия:**
1. Создать виджет `PostBottomSection`:
   - Аватар и имя автора
   - Кнопка "Подписаться" (toggle)
   - Описание с кликабельными @упоминаниями и #хештегами
   - Карточка услуги (если `post.service_id != null`)
   - Две CTA кнопки

2. Создать виджет `PostActionButtons`:
   - Вертикальная панель справа
   - Кнопки с счетчиками и анимацией

3. Добавить логику для видео:
   - Tap для pause/play
   - Прогресс-бар внизу видео
   - Индикатор "1/4" для множественных медиа

**Приоритет:** HIGH

---

## Фаза 2: Функциональные доработки

### 2.1 Список услуг мастера (Expand/Collapse)
**Файл:** `frontend/lib/features/profile/screens/master_profile_screen.dart`

**Требование:** Услуги должны быть свернуты по умолчанию с кнопкой "▼ Развернуть услуги"

**Действия:**
1. Создать `CollapsibleServicesList` виджет:
   - По умолчанию показывать только первые 3 услуги
   - Кнопка "Развернуть услуги (12)" для показа всех
   - Группировка по категориям
   - Клик на услугу → детальное описание с медиа

**Приоритет:** HIGH

---

### 2.2 Социальные функции (Друзья и подписки)
**Требуется:** Полная интеграция социальных связей

#### Backend:
**Файлы:** `backend/src/modules/friends/*`, `backend/src/modules/subscriptions/*`
- Уже реализованы, но нужна проверка endpoints

#### Frontend:
**Файлы (создать новые):**
- `frontend/lib/features/friends/screens/friends_screen.dart`
- `frontend/lib/features/friends/screens/friend_requests_screen.dart`
- `frontend/lib/features/subscriptions/screens/subscriptions_screen.dart`

**Действия:**
1. Создать экран "Друзья":
   - Табы: [Друзья] [Заявки (5)] [Поиск]
   - Список друзей с аватарами
   - Кнопка "Написать" для каждого друга

2. Создать экран "Подписки":
   - Табы: [Подписки (120)] [Подписчики (340)]
   - Возможность отписаться

3. Интегрировать в Feed:
   - Фильтр "Только друзья" должен показывать только посты друзей
   - Фильтр "Подписки" должен показывать только посты мастеров, на которых подписан

**Приоритет:** HIGH

---

### 2.3 Уведомления с фильтрацией
**Файл:** `frontend/lib/features/notifications/screens/notifications_screen.dart`

**Требование из дизайн-гайда:**
- Табы: [Все] [Записи] [Социальные] [Система]
- Группировка по датам (Сегодня, Вчера, 2 дня назад)
- Разные типы уведомлений с иконками

**Действия:**
1. Обновить backend endpoint `/notifications`:
   - Добавить параметр `type?: 'booking' | 'social' | 'system'`

2. Создать `NotificationsList` виджет:
   - Поддержка табов
   - Группировка по датам
   - Swipe для удаления
   - Кнопка "Отметить все как прочитанные"

**Приоритет:** MEDIUM

---

### 2.4 Чаты с расширенными возможностями
**Файлы:** `frontend/lib/features/chats/screens/*`

**Требования из дизайн-гайда:**
- Закрепленные чаты
- Поиск по чатам
- Отправка медиа (фото, видео, голосовые)
- Шаринг профилей мастеров и постов
- Системные сообщения (предложения записи)

**Действия:**
1. Добавить функцию закрепления:
   - Backend: добавить поле `is_pinned` в `chat_participants`
   - Frontend: Секция "Закрепленные" вверху списка

2. Создать `AttachmentPicker`:
   - Фото/видео из галереи
   - Камера
   - Шаринг профиля
   - Шаринг поста

3. Системные сообщения:
   - Backend: создать `SystemMessage` тип для предложений записи
   - Frontend: Отображать с кнопками действий

**Приоритет:** MEDIUM

---

### 2.5 Избранное (Bookmarks)
**Файлы (создать новые):**
- `backend/src/modules/favorites/*`
- `frontend/lib/features/favorites/*`

**Требования:**
- Пользователь может добавить в избранное: мастеров и посты
- Экран "Избранное" с табами [Мастера] [Посты]
- Кнопка "⭐" на постах и профилях

**Действия:**
1. Backend:
   - Создать таблицу `favorites` (user_id, entity_type, entity_id)
   - Endpoints: `POST /favorites`, `DELETE /favorites/:id`, `GET /favorites`

2. Frontend:
   - Создать `FavoritesScreen`
   - Интегрировать кнопку "⭐" в `PostDetailScreen` и `MasterProfileScreen`
   - Добавить фильтр "Избранные" в Feed

**Приоритет:** MEDIUM

---

## Фаза 3: UX-полировка и завершение

### 3.1 Создание поста (для мастеров)
**Файл (создать новый):** `frontend/lib/features/feed/screens/create_post_screen.dart`

**Требования:**
- Multi-step форма:
  1. Выбор медиа (до 10 фото/видео)
  2. Описание с поддержкой @упоминаний и #хештегов
  3. Отметка услуги (опционально)
  4. Настройки приватности (PUBLIC/FRIENDS/PRIVATE)

**Действия:**
1. Создать `CreatePostFlow` с 4 экранами
2. Интегрировать `image_picker` для выбора медиа
3. Использовать `PostRepository.createPost()`

**Приоритет:** MEDIUM

---

### 3.2 Автопредложения (Premium)
**Файлы:** Backend уже реализован, создать Frontend

**Требование:** Мастер может предлагать клиентам повторные записи

**Действия:**
1. Создать экран `AutoProposalsScreen`:
   - Список всех предложений (отправленные/принятые/отклоненные)
   - Кнопка "Создать предложение" на экране завершенной записи

2. Backend: проверить работу `/auto-proposals` endpoints

**Приоритет:** LOW (Premium feature)

---

### 3.3 Экран настроек
**Файл (создать новый):** `frontend/lib/features/settings/screens/settings_screen.dart`

**Разделы:**
- Аккаунт (редактирование профиля, смена пароля)
- Уведомления (push, email)
- Конфиденциальность (блокировки, скрытие профиля)
- Язык и регион
- О приложении
- Выход

**Приоритет:** MEDIUM

---

### 3.4 Тематические исправления
**Файл:** `frontend/lib/core/theme/app_theme.dart`

**Проблемы:**
- Inconsistent border radius (кнопки должны быть 100dp, карточки 12dp, inputs 8dp)
- Отсутствуют анимации с timing 200-300ms

**Действия:**
1. Стандартизировать border radius:
   ```dart
   ButtonTheme: BorderRadius.circular(100) // Fully rounded
   CardTheme: BorderRadius.circular(12)
   InputDecorationTheme: BorderRadius.circular(8)
   BottomSheetTheme: BorderRadius.vertical(top: Radius.circular(16))
   ```

2. Добавить глобальные transition durations:
   ```dart
   PageTransitionTheme: Duration(milliseconds: 250)
   ```

**Приоритет:** LOW

---

## Фаза 4: Тестирование и качество

### 4.1 Удаление TODO и замена print на логирование
**Файлы:** Все файлы с TODO комментариями

**Список TODO для удаления/исправления:**
1. `chat_screen.dart` (5 TODO) - реализовать информацию о собеседнике, online статус через WebSocket
2. `feed_screen.dart` (1 TODO) - реализован в Фазе 1.3
3. `friends_screen.dart` (3 TODO) - реализовать навигацию в Фазе 2.2
4. `login_screen.dart` (2 TODO) - Forgot password и Google OAuth

**Логирование:**
- Заменить все `print()` на `logger` (использовать пакет `logger`)
- Production: только ERROR и WARNING
- Development: DEBUG, INFO, WARNING, ERROR

**Приоритет:** MEDIUM

---

### 4.2 Интеграционные тесты (E2E)
**Папка:** `frontend/integration_test/`

**Критические сценарии для покрытия:**
1. **Регистрация и авторизация:**
   - Регистрация нового пользователя
   - Логин с валидными credentials
   - Логин с неверными credentials
   - Token refresh после 7 дней

2. **Обязательные отзывы:**
   - Попытка создать запись с незавершенными отзывами → должен показаться modal
   - Завершение записи мастером → должен требовать рейтинг

3. **Feed с геолокацией:**
   - Загрузка feed с радиусом 20 км
   - Переключение радиуса → новые посты
   - Фильтрация по категориям

4. **Booking flow:**
   - Создание записи → ожидание подтверждения
   - Подтверждение мастером → push уведомление
   - Завершение → обязательный отзыв

5. **Парсинг данных:**
   - Тест на response {data: [...], meta: {}}
   - Тест на прямой массив [...]
   - Тест на конвертацию String → Double для rating

**Файлы (создать):**
- `integration_test/auth_flow_test.dart`
- `integration_test/booking_flow_test.dart`
- `integration_test/feed_geolocation_test.dart`
- `integration_test/review_mandatory_test.dart`

**Приоритет:** HIGH

---

### 4.3 Unit тесты для критических компонентов
**Папка:** `frontend/test/`

**Покрытие:**
1. `test/repositories/` - Unit тесты для всех репозиториев
2. `test/providers/` - Unit тесты для Riverpod providers
3. `test/api/` - Тесты для `ApiHelpers`, `ApiExceptionHandler`
4. `test/models/` - Тесты для JSON serialization/deserialization

**Приоритет:** MEDIUM

---

## Фаза 5: Backend доработки

### 5.1 Forgot Password Flow
**Файлы:** Уже реализованы в backend (`auth.controller.ts`)

**Frontend действия:**
1. Создать экран `ForgotPasswordScreen`:
   - Ввод email
   - Отправка запроса на `/auth/forgot-password`
   - Показ сообщения "Проверьте email"

2. Создать экран `ResetPasswordScreen`:
   - Ввод нового пароля
   - Подтверждение пароля
   - Отправка на `/auth/reset-password` с токеном из email

**Приоритет:** MEDIUM

---

### 5.2 Google OAuth
**Статус:** ОТЛОЖЕНО на пост-MVP

**Решение:** Фокусируемся на критической функциональности. OAuth можно добавить после запуска MVP.

**Приоритет:** DEFERRED

---

## Критические файлы для изменений

### Backend (NestJS):
1. `backend/src/modules/bookings/bookings.service.ts` - Обязательные отзывы
2. `backend/src/modules/posts/posts.service.ts` - Геолокация в feed
3. `backend/src/modules/reviews/reviews.service.ts` - Автоматические отзывы
4. `backend/src/modules/favorites/*` - Новый модуль
5. `backend/src/modules/auth/auth.controller.ts` - OAuth (опционально)

### Frontend (Flutter):
1. `frontend/lib/core/theme/app_colors.dart` - Цветовая схема
2. `frontend/lib/core/theme/app_theme.dart` - Border radius, анимации
3. `frontend/lib/features/feed/screens/feed_screen.dart` - Фильтры, геолокация
4. `frontend/lib/features/feed/screens/post_detail_screen.dart` - Нижняя секция
5. `frontend/lib/features/bookings/screens/bookings_screen.dart` - Обязательные отзывы
6. `frontend/lib/core/repositories/booking_repository.dart` - Проверка отзывов
7. `frontend/lib/features/friends/*` - Новые экраны
8. `frontend/lib/features/favorites/*` - Новый модуль
9. `frontend/lib/features/feed/screens/create_post_screen.dart` - Создание постов

---

## Дополнительная документация

После завершения Спринта 2 создать:
- **TESTING.md** - Полное руководство по запуску E2E и unit тестов
- **DEPLOYMENT_CHECKLIST.md** - Чек-лист перед деплоем в production

**Содержание TESTING.md:**
1. Настройка тестового окружения
2. Запуск integration_test/ (E2E)
3. Запуск unit тестов
4. Интерпретация результатов
5. Добавление новых тестов
6. CI/CD интеграция

---

## Порядок реализации (приоритет)

### Спринт 1 (CRITICAL - 3-5 дней):
1. ✅ Цветовая схема Material 3
2. ✅ Обязательная система отзывов (Backend + Frontend)
3. ✅ Геолокационная фильтрация
4. ✅ Доработка экрана деталей поста

### Спринт 2 (HIGH - 5-7 дней):
5. ✅ Список услуг (expand/collapse)
6. ✅ Социальные функции (друзья, подписки)
7. ✅ Интеграционные E2E тесты
8. ✅ Удаление TODO, замена print на logger

### Спринт 3 (MEDIUM - 5-7 дней):
9. ✅ Уведомления с фильтрацией
10. ✅ Чаты с расширенными возможностями
11. ✅ Избранное (bookmarks)
12. ✅ Forgot Password
13. ✅ Экран настроек

### Спринт 4 (POLISH - 3-5 дней):
14. ✅ Создание поста (для мастеров)
15. ✅ Тематические исправления (border radius, анимации)
16. ✅ Unit тесты
17. ✅ Автопредложения (Premium)

---

## Верификация (End-to-End тестирование)

После каждого спринта:

1. **Ручное тестирование:**
   - Регистрация → создание записи → попытка создать еще одну без отзыва → modal блокировки
   - Feed → выбор радиуса 10 км → проверка что посты только в радиусе
   - Создание поста → загрузка 5 фото → публикация → проверка в feed

2. **Автоматизированные E2E тесты:**
   ```bash
   cd frontend
   flutter test integration_test/
   ```

3. **Backend API тесты:**
   ```bash
   cd backend
   npm run test:e2e
   ```

4. **Проверка соответствия дизайн-гайду:**
   - Открыть UXUI-Guide-v2.md
   - Пройти по каждому экрану и сравнить с реализацией
   - Чеклист соответствия (создать отдельный документ)

---

## Риски и mitigation

| Риск | Вероятность | Impact | Mitigation |
|------|-------------|--------|------------|
| Геолокация не работает на iOS | Средняя | Высокий | Тщательно настроить Info.plist, запросить разрешения |
| Backend медленно обрабатывает геозапросы | Средняя | Высокий | Добавить индекс на location_lat/lng, использовать PostGIS |
| E2E тесты нестабильны (flaky) | Высокая | Средний | Использовать явные ожидания, моки для внешних сервисов |
| Пользователи против обязательных отзывов | Низкая | Высокий | Добавить skip после 3 напоминаний (grace period) |
| Google OAuth сложен в настройке | Средняя | Низкий | Отложить на потом, начать с email/password |

---

## Итоговые метрики успеха

### Технические:
- ✅ 0 критических TODO в коде
- ✅ 0 использований `print()` в production коде
- ✅ E2E тесты покрывают 5 критических сценариев
- ✅ Unit тесты покрывают >50% репозиториев и providers
- ✅ Все цвета соответствуют Material 3 (#6750A4, #E91E63, #00BCD4)

### Бизнес-логика:
- ✅ Обязательные отзывы работают (блокировка без отзыва)
- ✅ Геолокационная фильтрация показывает посты в радиусе
- ✅ Социальные функции (друзья, подписки) полностью интегрированы
- ✅ Избранное доступно для постов и мастеров

### UX/UI:
- ✅ Соответствие UXUI-Guide-v2.md на 90%+
- ✅ Все анимации 200-300ms
- ✅ Border radius стандартизирован (100dp/12dp/8dp/16dp)
- ✅ Все экраны имеют корректные Empty States

---

## Заключение

Этот план покрывает **все критические проблемы**, выявленные при анализе проекта. Реализация займет **4 спринта (16-24 дня работы)** с приоритизацией критических багов и бизнес-логики.

**Ключевые улучшения:**
1. ✅ Цветовая схема → Material 3
2. ✅ Обязательные отзывы → защита качества сервиса
3. ✅ Геолокация → локальный discovery
4. ✅ Соответствие дизайн-гайду → профессиональный UX
5. ✅ E2E тесты → защита от регрессий

После реализации приложение будет **готово к MVP запуску** с полным соответствием дизайн-гайду и критическим бизнес-требованиям.
