# Краткое резюме исправлений

## ✅ Исправлено

### 1. TypeError: null is not a subtype of type 'String'
**Файл:** `frontend/lib/core/models/api/post_model.dart`
- Добавлены безопасные проверки в `CommentModel.fromJson`
- Все касты `as String` теперь имеют fallback значения

### 2. XFile.name может быть пустым на Web
**Файл:** `frontend/lib/core/repositories/post_repository.dart`
- Исправлена проверка `file.name` - теперь корректно обрабатывает пустую строку
- Используется fallback `'upload.jpg'` если имя файла пустое

### 3. Валидация перед созданием поста
**Файл:** `frontend/lib/features/feed/screens/create_post_screen.dart`
- Добавлена дополнительная проверка перед отправкой
- Предотвращает отправку поста без контента и медиа

---

## ⚠️ Требует проверки на бэкенде

### 1. 404 для `/masters/:id/services` и `/masters/:id/reviews`
**Проблема:** Эндпоинты не найдены

**Что проверить:**
```bash
# В backend/src/modules/masters/masters.controller.ts
# Должны быть роуты:
@Get(':id/services')
@Get(':id/reviews')
```

### 2. 400 Bad Request при создании поста
**Проблема:** Бэкенд отклоняет запрос

**Что проверить:**
1. Открыть **DevTools → Network → POST /api/v2/posts → Response**
2. Посмотреть детальное сообщение об ошибке от NestJS
3. Проверить валидацию в `CreatePostDto` на бэкенде

**Возможные причины:**
- Бэкенд требует непустой `content` ИЛИ наличие `media_urls`
- Неверный формат UUID в `service_ids`
- Отсутствуют обязательные поля

---

## 🔍 Как найти точную причину ошибки

### Для TypeError null:
1. Открыть **DevTools → Console**
2. Найти красную ошибку с полным stack trace
3. Определить, какая модель и какое поле вызывают проблему
4. Проверить **Network → Response** для этого запроса

### Для 400 Bad Request:
1. Открыть **DevTools → Network**
2. Найти запрос `POST /api/v2/posts`
3. Кликнуть на него → вкладка **Response**
4. Там будет детальное сообщение от NestJS:
   ```json
   {
     "statusCode": 400,
     "message": ["content should not be empty", "..."],
     "error": "Bad Request"
   }
   ```

---

## 📝 Следующие шаги

1. **Перезапустить приложение:**
   ```bash
   cd frontend && flutter run -d chrome
   ```

2. **Повторить тест создания поста:**
   - Выбрать фото
   - Добавить описание
   - Создать пост
   - Проверить ошибки в Console

3. **Если ошибка повторится:**
   - Открыть **DevTools → Network → Response**
   - Скопировать точное сообщение об ошибке
   - Исправить проблему на основе сообщения

---

## 📚 Дополнительная документация

- `docs/RUNTIME_ERRORS_FIX.md` - детальное описание всех ошибок и решений
- `docs/TESTING_CHECKLIST.md` - полный чек-лист для тестирования
- `docs/QUICK_TEST_START.md` - быстрый старт для тестирования
