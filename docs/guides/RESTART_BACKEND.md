# Инструкция по безопасному перезапуску бэкенда

## ✅ Важно: Данные не потеряются!

Все ваши данные хранятся в Docker volumes и **НЕ удаляются** при перезапуске:

- **База данных PostgreSQL**: `masters_postgres_data` - все посты, пользователи, профили мастеров
- **MinIO (изображения)**: `masters_minio_data` - все загруженные изображения
- **Redis (кэш)**: `masters_redis_data` - кэшированные данные
- **Meilisearch**: `masters_meilisearch_data` - индексы поиска

## 🔄 Варианты перезапуска

### Вариант 1: Простой перезапуск (без пересборки образа)

Если вы изменили только код TypeScript и хотите применить изменения:

```bash
# Перезапустить только контейнер бэкенда
docker-compose restart backend

# Или с пересозданием контейнера (если нужно применить изменения в .env)
docker-compose up -d --force-recreate backend
```

**Что происходит:**
- Контейнер бэкенда перезапускается
- Все volumes остаются подключенными
- Данные в БД и MinIO сохраняются
- Изменения в коде применяются (если используется volume mount для кода)

### Вариант 2: Пересборка образа (если изменился код)

Если вы изменили код и нужно пересобрать Docker образ:

```bash
# Пересобрать образ и перезапустить
docker-compose up -d --build backend
```

**Что происходит:**
- Собирается новый Docker образ с обновленным кодом
- Старый контейнер останавливается
- Создается новый контейнер с новым образом
- Все volumes остаются подключенными
- Данные сохраняются

### Вариант 3: Полный перезапуск всех сервисов

Если нужно перезапустить все сервисы (PostgreSQL, Redis, MinIO и т.д.):

```bash
# Остановить все сервисы
docker-compose stop

# Запустить все сервисы заново
docker-compose up -d
```

**Что происходит:**
- Все контейнеры останавливаются
- Все volumes остаются подключенными
- При запуске все контейнеры стартуют заново
- **Все данные сохраняются!**

## ⚠️ Что НЕ делать (чтобы не потерять данные)

### ❌ НЕ используйте эти команды:

```bash
# ЭТО УДАЛИТ ВСЕ ДАННЫЕ!
docker-compose down -v

# ЭТО ТОЖЕ УДАЛИТ ДАННЫЕ!
docker volume rm masters_postgres_data masters_minio_data
```

Эти команды удаляют volumes и **все данные будут потеряны**!

## 🔍 Проверка после перезапуска

После перезапуска проверьте:

```bash
# 1. Статус контейнеров
docker-compose ps

# 2. Логи бэкенда
docker-compose logs backend --tail 50

# 3. Проверка здоровья бэкенда
curl http://localhost:3000/api/v2/health

# 4. Проверка подключения к БД (в логах должно быть "Database connected")
docker-compose logs backend | grep -i "database\|connected"
```

## 📝 Текущая ситуация

Судя по статусу, ваш бэкенд сейчас в состоянии `unhealthy`. Это может быть из-за:
- Изменений в коде, которые нужно применить
- Проблем с подключением к БД
- Ошибок в коде

**Рекомендуемые действия:**

1. **Посмотрите логи**, чтобы понять проблему:
   ```bash
   docker-compose logs backend --tail 100
   ```

2. **Перезапустите бэкенд** после исправления кода:
   ```bash
   docker-compose restart backend
   ```

3. **Если нужно пересобрать образ**:
   ```bash
   docker-compose up -d --build backend
   ```

## 🎯 Быстрая команда для вашего случая

После изменений в `masters.service.ts`:

```bash
# Перезапустить бэкенд (применит изменения)
docker-compose restart backend

# Проверить логи
docker-compose logs backend --tail 50 -f
```

## 💾 Резервное копирование (опционально)

Если хотите быть уверены на 100%, можете сделать бэкап перед перезапуском:

```bash
# Бэкап PostgreSQL
docker-compose exec postgres pg_dump -U service_user service_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Бэкап MinIO (если нужно)
# MinIO данные уже в volume, но можно скопировать volume
```

Но это не обязательно - volumes сохраняют данные автоматически!
