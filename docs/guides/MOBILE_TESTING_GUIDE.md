# Руководство по тестированию на реальном Android устройстве

**Цель:** Подключить Android телефон к ПК и запустить Flutter приложение для локального тестирования

---

## 📱 Шаг 1: Подготовка Android устройства

### 1.1 Включите режим разработчика

1. Откройте **Настройки** на телефоне
2. Перейдите в **О телефоне** (About phone)
3. Найдите **Номер сборки** (Build number)
4. **Нажмите 7 раз** на "Номер сборки"
5. Вы увидите сообщение "Вы стали разработчиком!"

### 1.2 Включите отладку по USB

1. Вернитесь в **Настройки**
2. Найдите **Для разработчиков** (Developer options)
   - Может быть в разделе "Система" → "Дополнительно"
3. Включите переключатель **Отладка по USB** (USB debugging)
4. Подтвердите действие в диалоге

### 1.3 (Опционально) Отладка по Wi-Fi

Для беспроводного подключения:
1. В "Для разработчиков" включите **Беспроводная отладка** (Wireless debugging)
2. Запомните IP адрес и порт

---

## 🔌 Шаг 2: Подключение к ПК

### 2.1 USB подключение (рекомендуется)

1. Подключите телефон к ПК через **USB кабель**
2. На телефоне выберите режим **Передача файлов** (File transfer/MTP)
3. Должно появиться диалоговое окно "Разрешить отладку по USB?"
4. **Разрешите** и отметьте "Всегда разрешать с этого компьютера"

### 2.2 Проверка подключения

Откройте терминал и выполните:

```bash
# Проверить подключенные устройства
flutter devices
```

Должен появиться ваш телефон:
```
Android SDK built for x86 (mobile) • emulator-5554 • android-x86 • Android 11 (API 30)
SM-G973F (mobile) • 1234567890ABCDEF • android-arm64 • Android 12 (API 31)
```

Если устройство не найдено:

```bash
# Проверить ADB
adb devices

# Если пусто, перезапустить ADB сервер
adb kill-server
adb start-server
adb devices
```

---

## 🚀 Шаг 3: Запуск Backend (локально)

### 3.1 Настройка .env для локальной сети

Узнайте IP адрес вашего ПК:

**Windows:**
```bash
ipconfig
# Найдите IPv4 адрес (обычно 192.168.x.x)
```

**Linux/Mac:**
```bash
ifconfig
# или
ip addr show
```

### 3.2 Настройка backend/.env

Откройте `backend/.env` и убедитесь что настройки позволяют внешние подключения:

```env
# Сервер должен слушать на всех интерфейсах
HOST=0.0.0.0
PORT=3000

# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=service_platform

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=your-super-secret-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key

# CORS - разрешить запросы с мобильного
CORS_ORIGINS=http://localhost:3000,http://192.168.1.100:3000
```

### 3.3 Запуск сервисов

```bash
# 1. Запустить базу данных
cd backend
docker-compose up -d

# 2. Применить миграции (если нужно)
npm run migration:run

# 3. Запустить backend
npm run start:dev

# Backend должен быть доступен на http://192.168.x.x:3000
```

### 3.4 Проверка доступности

С телефона откройте браузер и перейдите:
```
http://192.168.x.x:3000/api/docs
```

Должна открыться Swagger документация.

---

## 📱 Шаг 4: Настройка Flutter приложения

### 4.1 Настройка API endpoint

Откройте `frontend/lib/core/constants/api_constants.dart`:

```dart
class ApiConstants {
  // Для локального тестирования используйте IP вашего ПК
  static const String baseUrl = 'http://192.168.1.100:3000'; // Замените на ваш IP
  
  // Для production
  // static const String baseUrl = 'https://api.yourapp.com';
  
  static const String apiVersion = '/api/v1';
  
  // Endpoints
  static const String auth = '/auth';
  static const String users = '/users';
  static const String posts = '/posts';
  // ...
}
```

### 4.2 Разрешение HTTP трафика (Android)

Android по умолчанию блокирует HTTP (только HTTPS). Для локальной разработки:

Создайте/обновите `frontend/android/app/src/main/res/xml/network_security_config.xml`:

```xml
<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Разрешить HTTP для локальной разработки -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.1.100</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
```

Обновите `frontend/android/app/src/main/AndroidManifest.xml`:

```xml
<application
    android:label="Service Platform"
    android:icon="@mipmap/ic_launcher"
    android:networkSecurityConfig="@xml/network_security_config">
    <!-- ... -->
</application>
```

---

## 🎯 Шаг 5: Запуск приложения

### 5.1 Запуск через CLI

```bash
cd frontend

# Убедитесь что устройство видно
flutter devices

# Запустить на подключенном устройстве
flutter run

# Или указать конкретное устройство
flutter run -d 1234567890ABCDEF
```

### 5.2 Запуск через VS Code

1. Откройте папку `frontend` в VS Code
2. Откройте любой `.dart` файл
3. В правом нижнем углу выберите ваше Android устройство
4. Нажмите **F5** или **Run → Start Debugging**

### 5.3 Hot Reload

После запуска можете использовать:
- **r** - hot reload (быстрая перезагрузка изменений)
- **R** - hot restart (полная перезагрузка)
- **q** - выход

---

## 🔧 Устранение проблем

### Проблема: Устройство не найдено

**Решение:**
```bash
# Проверить ADB
adb devices

# Если список пуст:
adb kill-server
adb start-server
adb devices

# Переподключить USB кабель
# Проверить что USB debugging включен на телефоне
```

### Проблема: "Connection refused" при запросах

**Решение:**
1. Проверьте что backend запущен: `http://192.168.x.x:3000/health`
2. Проверьте firewall на ПК (разрешите порт 3000)
3. Убедитесь что телефон и ПК в одной Wi-Fi сети
4. Проверьте IP адрес в ApiConstants

**Windows Firewall:**
```powershell
# Добавить правило для порта 3000
netsh advfirewall firewall add rule name="Node.js Server" dir=in action=allow protocol=TCP localport=3000
```

### Проблема: Gradle build failed

**Решение:**
```bash
cd frontend/android

# Очистить gradle cache
./gradlew clean

# Вернуться в frontend
cd ..

# Попробовать снова
flutter run
```

### Проблема: "Cleartext HTTP traffic not permitted"

**Решение:**
Убедитесь что `network_security_config.xml` создан и подключен в `AndroidManifest.xml` (см. Шаг 4.2)

---

## ✅ Чеклист готовности

Перед тестированием убедитесь:

- [ ] Режим разработчика включен на телефоне
- [ ] USB отладка включена
- [ ] Телефон подключен к ПК (flutter devices показывает устройство)
- [ ] Docker с PostgreSQL/Redis запущен
- [ ] Backend запущен (http://IP:3000/health отвечает)
- [ ] IP адрес ПК прописан в ApiConstants
- [ ] network_security_config.xml настроен
- [ ] Firewall разрешает порт 3000
- [ ] Телефон и ПК в одной сети

---

## 🎯 Быстрый старт (TL;DR)

```bash
# 1. На телефоне: включить USB debugging

# 2. Подключить USB кабель, разрешить отладку

# 3. Узнать IP ПК
ipconfig  # Windows
# Например: 192.168.1.100

# 4. Запустить backend
cd backend
docker-compose up -d
npm run start:dev

# 5. Настроить frontend
# В frontend/lib/core/constants/api_constants.dart:
# baseUrl = 'http://192.168.1.100:3000'

# 6. Создать network_security_config.xml (см. выше)

# 7. Запустить Flutter
cd frontend
flutter run
```

---

## 📚 Дополнительные ресурсы

- [Flutter: Run on physical device](https://docs.flutter.dev/get-started/install/windows#set-up-your-android-device)
- [Android Debug Bridge (ADB)](https://developer.android.com/studio/command-line/adb)
- [Network Security Config](https://developer.android.com/training/articles/security-config)

---

**Создано:** 14 января 2026  
**Версия:** 1.0  
**Для проекта:** Service Platform v2.0
