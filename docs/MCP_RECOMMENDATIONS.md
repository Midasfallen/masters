# Рекомендуемые MCP серверы для проекта

Этот документ содержит список полезных MCP серверов, которые могут ускорить разработку и отладку проекта.

## 🎯 Приоритетные MCP серверы

### 1. **GitHub MCP** ⭐⭐⭐⭐⭐
**Пакет:** `@modelcontextprotocol/server-github`

**Что делает:**
- Создание и управление Issues
- Просмотр и создание Pull Requests
- Работа с репозиторием (branches, commits, tags)
- Автоматизация workflow через GitHub Actions

**Почему полезно:**
- Автоматическое создание issues при обнаружении багов
- Просмотр истории коммитов без выхода из Cursor
- Управление PR прямо из IDE

**Установка:**
```bash
cd backend
npm install --save-dev @modelcontextprotocol/server-github
```

**Конфигурация:**
```json
{
  "github": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-github"],
    "env": {
      "GITHUB_PERSONAL_ACCESS_TOKEN": "your_token_here"
    }
  }
}
```

---

### 2. **Git MCP** ⭐⭐⭐⭐⭐
**Пакет:** `@modelcontextprotocol/server-git`

**Что делает:**
- Выполнение git команд напрямую
- Просмотр diff, log, status
- Создание коммитов и веток
- Работа с stash

**Почему полезно:**
- Быстрое создание коммитов с правильными сообщениями
- Просмотр изменений перед коммитом
- Управление ветками без терминала

**Установка:**
```bash
cd backend
npm install --save-dev @modelcontextprotocol/server-git
```

**Конфигурация:**
```json
{
  "git": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-git"],
    "env": {
      "GIT_WORKSPACE": "C:\\masters\\masters"
    }
  }
}
```

---

### 3. **Docker MCP** ⭐⭐⭐⭐
**Пакет:** `@modelcontextprotocol/server-docker` (если существует) или кастомный

**Что делает:**
- Управление Docker контейнерами
- Просмотр логов контейнеров
- Проверка статуса сервисов
- Перезапуск контейнеров

**Почему полезно:**
- Быстрая проверка статуса PostgreSQL, Redis, MinIO
- Просмотр логов без `docker logs`
- Перезапуск сервисов при необходимости

**Альтернатива:** Используйте существующие команды через `filesystem` MCP для чтения `docker-compose.yml` и выполнения команд.

---

### 4. **Brave Search MCP** ⭐⭐⭐⭐
**Пакет:** `@modelcontextprotocol/server-brave-search`

**Что делает:**
- Поиск документации по NestJS, Flutter, TypeORM
- Поиск решений проблем на Stack Overflow
- Поиск примеров кода

**Почему полезно:**
- Быстрый поиск документации без переключения в браузер
- Поиск решений ошибок прямо в контексте разработки
- Актуальная информация о библиотеках

**Установка:**
```bash
cd backend
npm install --save-dev @modelcontextprotocol/server-brave-search
```

**Конфигурация:**
```json
{
  "brave-search": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-brave-search"],
    "env": {
      "BRAVE_API_KEY": "your_api_key_here"
    }
  }
}
```

**Получение API ключа:** https://brave.com/search/api/

---

### 5. **Memory MCP** ⭐⭐⭐⭐
**Пакет:** `@modelcontextprotocol/server-memory`

**Что делает:**
- Сохранение контекста между сессиями
- Запоминание важных решений и архитектурных решений
- Хранение информации о багах и их решениях

**Почему полезно:**
- Не нужно повторно объяснять контекст проекта
- Сохранение знаний о проекте
- Быстрый доступ к предыдущим решениям

**Установка:**
```bash
cd backend
npm install --save-dev @modelcontextprotocol/server-memory
```

**Конфигурация:**
```json
{
  "memory": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-memory"],
    "env": {
      "MEMORY_STORAGE_PATH": "C:\\masters\\masters\\.cursor\\memory"
    }
  }
}
```

---

### 6. **Playwright MCP** ⭐⭐⭐⭐
**Пакет:** `@modelcontextprotocol/server-playwright`

**Что делает:**
- Более современная альтернатива Puppeteer
- Скриншоты и видео записи тестов
- Автоматизация браузерных тестов
- Лучшая поддержка современных браузеров

**Почему полезно:**
- Более стабильная работа с Flutter Web
- Лучшая поддержка Chrome DevTools Protocol
- Запись видео для отладки

**Установка:**
```bash
cd backend
npm install --save-dev @modelcontextprotocol/server-playwright
```

**Конфигурация:**
```json
{
  "playwright": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-playwright"],
    "env": {
      "BROWSER": "chromium"
    }
  }
}
```

---

## 🔧 Специализированные MCP серверы

### 7. **Redis MCP** ⭐⭐⭐
**Пакет:** Кастомный или через существующие инструменты

**Что делает:**
- Просмотр кеша Redis
- Очистка кеша
- Проверка ключей и значений
- Мониторинг производительности

**Почему полезно:**
- Отладка проблем с кешированием
- Проверка данных в Redis без redis-cli
- Очистка кеша при необходимости

**Альтернатива:** Используйте `postgres` MCP для прямых SQL запросов к Redis через Redis Commander или создайте кастомный скрипт.

---

### 8. **Slack/Discord MCP** ⭐⭐⭐
**Пакет:** `@modelcontextprotocol/server-slack` (если используется)

**Что делает:**
- Отправка уведомлений в Slack/Discord
- Интеграция с командой
- Автоматические алерты о деплоях

**Почему полезно:**
- Уведомления команды о важных изменениях
- Интеграция CI/CD с коммуникацией
- Автоматизация workflow

**Когда использовать:** Только если команда активно использует Slack/Discord для коммуникации.

---

### 9. **Linear/Jira MCP** ⭐⭐⭐
**Пакет:** `@modelcontextprotocol/server-linear` или `@modelcontextprotocol/server-jira`

**Что делает:**
- Создание и управление задачами
- Синхронизация с GitHub Issues
- Отслеживание прогресса

**Почему полезно:**
- Автоматическое создание задач из багов
- Синхронизация задач между системами
- Отслеживание прогресса разработки

**Когда использовать:** Если команда использует Linear или Jira для управления проектами.

---

### 10. **SQLite MCP** ⭐⭐
**Пакет:** `@modelcontextprotocol/server-sqlite`

**Что делает:**
- Работа с SQLite базами данных
- Полезно для тестовых БД
- Быстрое прототипирование

**Почему полезно:**
- Тестовые базы данных для быстрого прототипирования
- Локальное тестирование без Docker
- Простая миграция данных

**Когда использовать:** Для локального тестирования или прототипирования новых фич.

---

## 📊 Сравнительная таблица

| MCP Сервер | Приоритет | Сложность установки | Полезность |
|------------|-----------|-------------------|------------|
| GitHub MCP | ⭐⭐⭐⭐⭐ | Низкая | Очень высокая |
| Git MCP | ⭐⭐⭐⭐⭐ | Низкая | Очень высокая |
| Memory MCP | ⭐⭐⭐⭐ | Низкая | Высокая |
| Brave Search MCP | ⭐⭐⭐⭐ | Средняя | Высокая |
| Playwright MCP | ⭐⭐⭐⭐ | Средняя | Высокая |
| Docker MCP | ⭐⭐⭐ | Средняя | Средняя |
| Redis MCP | ⭐⭐⭐ | Высокая | Средняя |
| Slack/Discord MCP | ⭐⭐⭐ | Средняя | Средняя |
| Linear/Jira MCP | ⭐⭐⭐ | Средняя | Средняя |
| SQLite MCP | ⭐⭐ | Низкая | Низкая |

---

## 🚀 Быстрая установка (Топ-5)

Для быстрого старта установите эти 5 MCP серверов:

```bash
cd backend
npm install --save-dev \
  @modelcontextprotocol/server-github \
  @modelcontextprotocol/server-git \
  @modelcontextprotocol/server-memory \
  @modelcontextprotocol/server-brave-search \
  @modelcontextprotocol/server-playwright
```

Затем добавьте их в `.cursor/mcp.json` (см. примеры конфигурации выше).

---

## 📝 Рекомендации по использованию

1. **Начните с GitHub и Git MCP** — они дадут максимальную пользу сразу
2. **Добавьте Memory MCP** — для сохранения контекста проекта
3. **Brave Search MCP** — для быстрого поиска документации
4. **Playwright MCP** — замените Puppeteer на более современное решение
5. **Остальные по необходимости** — добавляйте по мере необходимости

---

## 🔗 Полезные ссылки

- [Официальный список MCP серверов](https://github.com/modelcontextprotocol/servers)
- [MCP Documentation](https://modelcontextprotocol.io/)
- [Cursor MCP Setup](https://docs.cursor.com/mcp)
