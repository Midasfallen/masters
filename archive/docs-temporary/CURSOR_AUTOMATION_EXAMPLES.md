# 🤖 Примеры автоматизации в Cursor

Практические примеры использования автоматизации для повышения эффективности.

---

## 📋 Содержание

1. [Автоматизация через промпты](#автоматизация-через-промпты)
2. [Использование Tasks](#использование-tasks)
3. [Работа с MCP серверами](#работа-с-mcp-серверами)
4. [Snippets в действии](#snippets-в-действии)
5. [Автоматизация Git](#автоматизация-git)

---

## 💬 Автоматизация через промпты

### Пример 1: Создание нового модуля

**Промпт:**
```
Создай новый модуль Notifications в backend:

1. Создай структуру:
   - notifications.module.ts
   - notifications.controller.ts
   - notifications.service.ts
   - dto/create-notification.dto.ts
   - entities/notification.entity.ts

2. Используй паттерны из существующих модулей (например, posts)
3. Добавь валидацию через class-validator
4. Добавь Swagger документацию
5. Интегрируй с TypeORM

Результат должен быть готов к использованию без дополнительных правок.
```

**Результат:** AI создаст весь модуль со всеми необходимыми файлами.

---

### Пример 2: Исправление бага с контекстом

**Промпт:**
```
Баг: При создании поста возвращается 400 Bad Request

Контекст:
- Файл: backend/src/modules/posts/posts.controller.ts
- Ошибка: "property media_urls should not exist"
- Backend ожидает: CreatePostDto с полем media (массив)

Исправь:
1. Проверь CreatePostDto в backend/src/modules/posts/dto/create-post.dto.ts
2. Обнови frontend/lib/core/models/api/post_model.dart
3. Убедись, что фронтенд отправляет правильную структуру

Проверь все связанные файлы перед исправлением.
```

**Результат:** AI найдет и исправит проблему во всех связанных файлах.

---

### Пример 3: Рефакторинг с сохранением функциональности

**Промпт:**
```
Рефакторинг: Вынеси общую логику валидации в отдельный сервис

Текущая ситуация:
- Валидация email дублируется в users.service.ts и auth.service.ts
- Валидация UUID дублируется в нескольких контроллерах

Задача:
1. Создай ValidationService в common/services/validation.service.ts
2. Вынеси общие методы валидации
3. Обнови все места использования
4. Убедись, что тесты проходят

Используй паттерн из существующих common сервисов.
```

**Результат:** AI создаст сервис и обновит все использования.

---

## 🔧 Использование Tasks

### Быстрый запуск разработки

**Комбинация клавиш:** `Ctrl+Shift+B`

**Что происходит:**
1. Запускается Docker инфраструктура
2. Запускается Backend в dev режиме
3. Запускается Frontend на Web

**Экономия времени:** ~2 минуты → 10 секунд

---

### Автоматическое тестирование

**Комбинация клавиш:** `Ctrl+Shift+T`

**Что происходит:**
1. Запускаются unit тесты Backend
2. Запускаются unit тесты Frontend
3. Показываются результаты в панели

**Экономия времени:** Ручной запуск → Автоматический

---

## 🌐 Работа с MCP серверами

### Пример 1: Автоматическое создание Issue

**Промпт:**
```
AI, создай GitHub issue для бага:

Заголовок: "Ошибка валидации при создании поста"
Описание: "При отправке POST /posts возвращается 400 Bad Request"
Лейблы: ["bug", "backend"]
Приоритет: High

Используй GitHub MCP для создания issue.
```

**Результат:** Issue автоматически создается в репозитории.

---

### Пример 2: Проверка данных в БД

**Промпт:**
```
AI, проверь через Postgres MCP:
- Сколько пользователей в базе?
- Есть ли посты без медиа?
- Какие категории услуг используются чаще всего?

Выведи результаты в таблице.
```

**Результат:** AI выполнит SQL запросы и покажет результаты.

---

### Пример 3: Работа с Git

**Промпт:**
```
AI, используй Git MCP:
1. Покажи статус репозитория
2. Покажи последние 5 коммитов
3. Создай коммит с сообщением "Fix: исправлена валидация email"

Используй Git MCP для всех операций.
```

**Результат:** AI выполнит все git операции автоматически.

---

## 📝 Snippets в действии

### Создание NestJS контроллера

**Действие:** Наберите `nest-controller` и нажмите Tab

**Результат:**
```typescript
import { Controller, Get, Post, Body, Param, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiResponse } from '@nestjs/swagger';
import { JwtAuthGuard } from '../common/guards/jwt-auth.guard';

@ApiTags('resource')
@Controller('resource')
@UseGuards(JwtAuthGuard)
export class ResourceController {
  constructor(private readonly service: ResourceService) {}

  @Get()
  @ApiOperation({ summary: 'Get all resource' })
  async findAll() {
    return this.service.findAll();
  }
}
```

**Экономия времени:** 2 минуты → 10 секунд

---

### Создание Flutter экрана

**Действие:** Наберите `flutter-screen` и нажмите Tab

**Результат:**
```dart
import 'package:flutter/material.dart';

class ScreenNameScreen extends StatelessWidget {
  const ScreenNameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Title'),
      ),
      body: const Center(
        child: Text('ScreenName'),
      ),
    );
  }
}
```

**Экономия времени:** 1 минута → 5 секунд

---

## 🔄 Автоматизация Git

### Автоматический коммит через Git MCP

**Промпт:**
```
AI, создай коммит через Git MCP:
- Сообщение: "feat: добавлена валидация email в DTO"
- Файлы: все измененные файлы
- Проверь статус перед коммитом
```

**Результат:** AI проверит статус, добавит файлы и создаст коммит.

---

### Автоматическое создание PR

**Промпт:**
```
AI, создай Pull Request через GitHub MCP:
- Заголовок: "Feature: Добавлена валидация email"
- Описание: "Добавлена валидация email во всех DTO с использованием class-validator"
- База: main
- Ветка: feature/email-validation
```

**Результат:** PR автоматически создается в GitHub.

---

## 🎯 Комбинированные сценарии

### Полный цикл разработки фичи

**Промпт:**
```
Полный цикл разработки фичи "Уведомления":

1. Создай структуру модуля через snippets (nest-module)
2. Реализуй CRUD операции
3. Добавь тесты
4. Запусти тесты через Task (Ctrl+Shift+T)
5. Если тесты проходят - создай коммит через Git MCP
6. Создай PR через GitHub MCP

Используй все доступные инструменты автоматизации.
```

**Результат:** Вся фича разработана и готова к ревью автоматически.

---

## 📊 Метрики эффективности

### До оптимизации:
- Создание модуля: ~30 минут
- Исправление бага: ~15 минут
- Рефакторинг: ~45 минут

### После оптимизации:
- Создание модуля: ~5 минут (через snippets + AI)
- Исправление бага: ~3 минуты (через контекстный промпт)
- Рефакторинг: ~10 минут (через автоматизацию)

**Улучшение:** В 3-6 раз быстрее! 🚀

---

## ✅ Чек-лист использования

- [ ] Использую структурированные промпты
- [ ] Использую Tasks для запуска сервисов
- [ ] Использую Snippets для быстрого создания кода
- [ ] Использую MCP серверы для автоматизации
- [ ] Проверяю результаты перед коммитом
- [ ] Использую комбинированные сценарии для сложных задач

---

**Помните:** Автоматизация - это инструмент. Используйте его разумно и всегда проверяйте результаты!
