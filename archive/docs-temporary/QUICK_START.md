# 🚀 Быстрый старт для тестирования

## Предварительные требования

### 1. Проверка Docker контейнеров
Убедитесь, что инфраструктура запущена:
```bash
docker-compose ps
```

Должны быть запущены:
- ✅ `service_postgres` (PostgreSQL на порту 5433)
- ✅ `service_minio` (MinIO на порту 9000)
- ✅ `service_redis` (Redis на порту 6379)

Если контейнеры не запущены:
```bash
docker-compose up -d
```

### 2. Проверка контейнера бэкенда
**ВАЖНО:** Перед запуском локального бэкенда ОБЯЗАТЕЛЬНО остановите контейнер бэкенда:
```bash
docker stop service_backend
```

Два одновременно запущенных бэкенда (в докере и локально) гарантируют конфликты портов!

---

## Запуск Backend

### Шаг 1: Переход в директорию
```bash
cd backend
```

### Шаг 2: Проверка зависимостей
```bash
npm install
```

Если зависимости уже установлены, этот шаг можно пропустить.

### Шаг 3: Запуск dev сервера
```bash
npm run start:dev
```

**Ожидаемый результат:**
- Сервер запускается на `http://localhost:3000`
- Swagger UI доступен на `http://localhost:3000/api/v2/docs`
- В консоли видны логи подключения к БД и запуска модулей

**Если возникли ошибки:**
- Проверьте, что Docker контейнеры запущены (`docker-compose ps`)
- Проверьте подключение к БД: `docker logs service_postgres`
- Убедитесь, что порт 3000 свободен

---

## Запуск Frontend

### Шаг 1: Переход в директорию
```bash
cd frontend
```

### Шаг 2: Проверка зависимостей
```bash
flutter pub get
```

Если зависимости уже установлены, этот шаг можно пропустить.

### Шаг 3: Проверка кода (опционально)
```bash
flutter analyze
```

### Шаг 4: Запуск на Chrome
```bash
flutter run -d chrome
```

**Ожидаемый результат:**
- Приложение открывается в Chrome
- DevTools доступны (F12)
- Hot Reload работает при изменении кода

**Если возникли ошибки:**
- Проверьте, что Chrome установлен и доступен
- Убедитесь, что Flutter Web включен: `flutter config --enable-web`
- Проверьте версию Flutter: `flutter --version` (должна быть 3.35+)

---

## Проверка работоспособности

### Backend
1. Откройте Swagger UI: `http://localhost:3000/api/v2/docs`
2. Попробуйте авторизоваться через `/auth/login`
3. Проверьте Health Check: `http://localhost:3000/health`

### Frontend
1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Попробуйте авторизоваться в приложении
4. Проверьте, что запросы уходят на `http://localhost:3000/api/v2`

---

## Типичные проблемы

### Backend не запускается

**Ошибка:** `ECONNREFUSED` или `relation does not exist`
- **Решение:** Проверьте, что PostgreSQL контейнер запущен: `docker-compose ps`
- **Решение:** Примените миграции: `npm run migration:run`

**Ошибка:** `Port 3000 is already in use`
- **Решение:** Остановите контейнер бэкенда: `docker stop service_backend`
- **Решение:** Или найдите процесс на порту 3000 и завершите его

### Frontend не подключается к Backend

**Ошибка:** CORS ошибки в консоли
- **Решение:** Убедитесь, что бэкенд запущен на `http://localhost:3000`
- **Решение:** Проверьте конфигурацию CORS в `backend/src/main.ts`

**Ошибка:** `Failed to load resource: net::ERR_CONNECTION_REFUSED`
- **Решение:** Убедитесь, что бэкенд запущен: `curl http://localhost:3000/health`
- **Решение:** Проверьте URL API в `frontend/lib/core/config/app_config.dart`

### Flutter Web не запускается

**Ошибка:** `Could not find an option named "--web-renderer"`
- **Решение:** В Flutter 3.35+ этот флаг удален, используйте просто `flutter run -d chrome`

**Ошибка:** `Chrome not found`
- **Решение:** Установите Chrome или используйте другой браузер: `flutter run -d web-server`

---

## Полезные команды

### Backend
```bash
# Просмотр логов
docker logs service_postgres --tail 50
docker logs service_minio --tail 50

# Применить миграции
npm run migration:run

# Заполнить тестовыми данными
npm run seed

# Перезапуск
# Ctrl+C для остановки, затем снова npm run start:dev
```

### Frontend
```bash
# Очистка кэша
flutter clean
flutter pub get

# Регенерация кода (если изменились модели)
flutter pub run build_runner build --delete-conflicting-outputs

# Проверка устройств
flutter devices
```

### Docker
```bash
# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Перезапуск контейнеров
docker-compose restart

# Остановка всех контейнеров
docker-compose down
```

---

## Порты и URL

| Сервис | Порт | URL |
|--------|------|-----|
| Backend API | 3000 | http://localhost:3000/api/v2 |
| Swagger UI | 3000 | http://localhost:3000/api/v2/docs |
| PostgreSQL | 5433 | localhost:5433 |
| MinIO | 9000 | http://localhost:9000 |
| Redis | 6379 | localhost:6379 |
| Flutter Web | 58427+ | http://localhost:58427 (динамический) |

---

## Следующие шаги

После успешного запуска:
1. ✅ Протестируйте создание поста (исправлено в последнем коммите)
2. ✅ Проверьте загрузку медиафайлов
3. ✅ Протестируйте лайки и комментарии
4. ✅ Проверьте работу аватаров с fallback

Документация по тестированию:
- `docs/TESTING_CHECKLIST.md` - полный чек-лист
- `docs/QUICK_TEST_START.md` - быстрый старт для тестирования
- `docs/CREATE_POST_FIX.md` - исправление создания поста
