# 📋 Инструкция после мерджа PR в main

## ⚠️ ВАЖНО: Выполните эти шаги после мерджа PR

После того как PR будет смержен в main, необходимо выполнить следующие действия на вашем локальном компьютере.

---

## 🔄 Шаг 1: Обновление локального репозитория

### 1.1 Переключитесь на ветку main
```bash
cd /path/to/masters
git checkout main
```

### 1.2 Получите последние изменения
```bash
git pull origin main
```

**Ожидаемый результат:** Вы должны увидеть все изменения из PR:
- Обновление `frontend/lib/core/config/app_config.dart`
- Обновление 7 файлов моделей с @JsonKey аннотациями
- Обновление `frontend/lib/core/repositories/auth_repository.dart`
- Добавление `PHASE_3_PROGRESS.md`
- Добавление `SESSION_SUMMARY_PHASE3.md`

### 1.3 Проверьте статус
```bash
git status
```

**Ожидаемый вывод:** `Your branch is up to date with 'origin/main'`

---

## 📱 Шаг 2: Генерация Freezed кода (КРИТИЧНО!)

### 2.1 Проверьте установку Flutter

```bash
flutter --version
```

**Требуемая версия:** Flutter 3.2.0 или выше

**Если Flutter не установлен:**
1. Скачайте с https://docs.flutter.dev/get-started/install
2. Следуйте инструкциям для вашей ОС (Windows/macOS/Linux)
3. Добавьте Flutter в PATH
4. Выполните `flutter doctor` для проверки

### 2.2 Перейдите в директорию frontend

```bash
cd frontend
```

### 2.3 Очистите предыдущие сборки (опционально, но рекомендуется)

```bash
flutter clean
```

### 2.4 Установите зависимости

```bash
flutter pub get
```

**Ожидаемый результат:** Все пакеты из `pubspec.yaml` будут загружены, включая:
- freezed
- freezed_annotation
- json_annotation
- json_serializable
- build_runner
- riverpod
- dio
- и другие

### 2.5 Генерация Freezed кода

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

**Что делает эта команда:**
- Генерирует `.freezed.dart` файлы (immutable data classes)
- Генерирует `.g.dart` файлы (JSON serialization/deserialization)
- Удаляет конфликтующие старые файлы

**Ожидаемый результат:** Будет создано ~14 новых файлов:

```
frontend/lib/core/models/api/
├── user_model.freezed.dart
├── user_model.g.dart
├── master_model.freezed.dart
├── master_model.g.dart
├── service_model.freezed.dart
├── service_model.g.dart
├── booking_model.freezed.dart
├── booking_model.g.dart
├── review_model.freezed.dart
├── review_model.g.dart
├── chat_model.freezed.dart
├── chat_model.g.dart
├── post_model.freezed.dart
└── post_model.g.dart
```

**Процесс займет:** 1-3 минуты

**Если возникают ошибки:**
```bash
# Очистка и повторная попытка
flutter clean
rm -rf .dart_tool
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```

### 2.6 Проверка сгенерированных файлов

```bash
# Проверьте, что файлы созданы
ls -la lib/core/models/api/*.freezed.dart
ls -la lib/core/models/api/*.g.dart
```

**Ожидаемый вывод:** Список всех сгенерированных файлов

---

## 🔍 Шаг 3: Проверка кода на ошибки

### 3.1 Анализ кода

```bash
flutter analyze
```

**Ожидаемый результат:** `No issues found!`

**Если есть предупреждения/ошибки:**
- Прочитайте внимательно сообщения
- Исправьте проблемы в соответствующих файлах
- Запустите `flutter analyze` снова

### 3.2 Проверка форматирования

```bash
dart format lib/ --set-exit-if-changed
```

**Если код не отформатирован:**
```bash
dart format lib/ --output=write
```

### 3.3 Попытка сборки (без запуска)

```bash
# Для Android
flutter build apk --debug --no-pub

# Для iOS (только на macOS)
flutter build ios --debug --no-codesign --no-pub
```

**Ожидаемый результат:** Сборка проходит успешно без ошибок

**Если есть ошибки компиляции:**
1. Прочитайте стек ошибки
2. Проверьте, что все `.freezed.dart` и `.g.dart` файлы сгенерированы
3. Проверьте, что все импорты корректны
4. Повторите генерацию Freezed кода

---

## ✅ Шаг 4: Коммит сгенерированных файлов

### 4.1 Проверьте изменения

```bash
git status
```

**Ожидаемый вывод:** ~14 новых файлов (.freezed.dart и .g.dart)

### 4.2 Добавьте сгенерированные файлы

```bash
git add lib/core/models/api/*.freezed.dart
git add lib/core/models/api/*.g.dart
```

### 4.3 Создайте коммит

```bash
git commit -m "build: Сгенерированы Freezed и JSON serialization файлы

- Добавлены .freezed.dart файлы для всех моделей
- Добавлены .g.dart файлы для JSON сериализации
- Выполнено после мерджа PR с обновлениями моделей

Команда: flutter pub run build_runner build --delete-conflicting-outputs"
```

### 4.4 Отправьте изменения в remote

```bash
git push origin main
```

**Ожидаемый результат:** Сгенерированные файлы добавлены в main ветку

---

## 🚀 Шаг 5: Проверка работоспособности

### 5.1 Запустите Backend

**Откройте новый терминал:**
```bash
cd /path/to/masters/backend
npm run start:dev
```

**Ожидаемый вывод:**
```
[Nest] INFO  [NestFactory] Starting Nest application...
[Nest] INFO  Application is running on: http://localhost:3000
[Nest] INFO  Swagger documentation: http://localhost:3000/api/docs
```

**Проверьте Swagger:** Откройте http://localhost:3000/api/docs в браузере

### 5.2 Запустите Frontend

**В другом терминале:**
```bash
cd /path/to/masters/frontend

# Для Android эмулятора
flutter run

# Для конкретного устройства
flutter devices  # Посмотрите список устройств
flutter run -d <device-id>

# Для Chrome (web)
flutter run -d chrome
```

**Ожидаемый результат:** Приложение запустится без ошибок компиляции

---

## 🧪 Шаг 6: Базовое тестирование

### 6.1 Проверьте подключение к API

В приложении:
1. Откройте экран регистрации/входа
2. Попробуйте зарегистрировать нового пользователя
3. Проверьте, что запросы отправляются на `http://localhost:3000/api/v2`

**В логах Flutter должны быть:**
```
[DIO] Request: POST /api/v2/auth/register
[DIO] Response: 201 Created
```

**В логах Backend должны быть:**
```
[Nest] INFO  POST /api/v2/auth/register 201
```

### 6.2 Проверьте JSON сериализацию

Попробуйте выполнить несколько операций:
1. Регистрация пользователя
2. Вход в систему
3. Получение списка мастеров
4. Просмотр профиля мастера

**Если данные отображаются корректно** → JSON сериализация работает!

**Если видите ошибки типа:**
- `type 'String' is not a subtype of type 'int'`
- `Null check operator used on a null value`
- `Missing required fields`

→ Проверьте, что @JsonKey аннотации добавлены для всех полей

---

## 📝 Шаг 7: Следующие задачи (опционально)

После успешной генерации кода и проверки, можно приступить к следующим задачам:

### 7.1 Создание провайдеров

**MastersProvider** (`lib/core/providers/api/masters_provider.dart`):
```bash
# Создайте файл и добавьте код из SESSION_SUMMARY_PHASE3.md (раздел "Шаг 3.1")
```

**BookingsProvider** (`lib/core/providers/api/bookings_provider.dart`):
```bash
# Создайте файл и добавьте код из SESSION_SUMMARY_PHASE3.md (раздел "Шаг 3.2")
```

### 7.2 Интеграция UI с API

**LoginScreen** (`lib/features/auth/screens/login_screen.dart`):
```bash
# Обновите код согласно примеру из SESSION_SUMMARY_PHASE3.md (раздел "Шаг 4.1")
```

### 7.3 Написание тестов

```bash
# Unit tests
cd frontend
flutter test test/repositories/auth_repository_test.dart

# Integration tests
flutter test integration_test/auth_flow_test.dart

# Widget tests
flutter test test/widgets/login_screen_test.dart
```

---

## 🆘 Устранение проблем

### Проблема: Flutter не найден

**Решение:**
```bash
# macOS/Linux
export PATH="$PATH:/path/to/flutter/bin"
echo 'export PATH="$PATH:/path/to/flutter/bin"' >> ~/.bashrc

# Windows (PowerShell)
$env:Path += ";C:\path\to\flutter\bin"
```

### Проблема: build_runner зависает

**Решение:**
```bash
# Остановите процесс (Ctrl+C)
flutter clean
rm -rf .dart_tool
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs --verbose
```

### Проблема: Ошибки генерации Freezed

**Типичная ошибка:**
```
[WARNING] Missing `@JsonSerializable` annotation
```

**Решение:**
Убедитесь, что все модели имеют:
```dart
part 'model_name.freezed.dart';
part 'model_name.g.dart';

@freezed
class ModelName with _$ModelName {
  const factory ModelName({...}) = _ModelName;

  factory ModelName.fromJson(Map<String, dynamic> json) =>
      _$ModelNameFromJson(json);
}
```

### Проблема: Backend не отвечает

**Решение:**
```bash
# Проверьте, что Backend запущен
curl http://localhost:3000/api/v2/health

# Проверьте логи
cd backend
npm run start:dev
```

### Проблема: CORS ошибки

**Решение:**
В `backend/src/main.ts` проверьте настройки CORS:
```typescript
app.enableCors({
  origin: true,
  credentials: true,
});
```

---

## 📊 Чеклист выполнения

- [ ] Переключился на main ветку
- [ ] Выполнил `git pull origin main`
- [ ] Проверил установку Flutter (`flutter --version`)
- [ ] Перешел в директорию frontend
- [ ] Выполнил `flutter clean`
- [ ] Выполнил `flutter pub get`
- [ ] Запустил `flutter pub run build_runner build --delete-conflicting-outputs`
- [ ] Проверил, что создано ~14 файлов (.freezed.dart и .g.dart)
- [ ] Выполнил `flutter analyze` (нет ошибок)
- [ ] Попытался собрать приложение (`flutter build apk --debug --no-pub`)
- [ ] Закоммитил сгенерированные файлы
- [ ] Запушил изменения в main
- [ ] Запустил Backend на порту 3000
- [ ] Запустил Frontend
- [ ] Проверил работу регистрации/входа
- [ ] Проверил JSON сериализацию

---

## 📚 Дополнительные ресурсы

### Документация
- **Flutter:** https://docs.flutter.dev
- **Freezed:** https://pub.dev/packages/freezed
- **Riverpod:** https://riverpod.dev
- **Dio:** https://pub.dev/packages/dio

### Отчеты по проекту
- **PHASE_3_PROGRESS.md** - подробный отчет о прогрессе ФАЗЫ 3
- **SESSION_SUMMARY_PHASE3.md** - итоговый отчет с примерами кода
- **PHASE_2_COMPLETED.md** - отчет о завершении ФАЗЫ 2 (Backend)

### Контакты для помощи
Если возникли сложности:
1. Проверьте логи Flutter и Backend
2. Изучите примеры в SESSION_SUMMARY_PHASE3.md
3. Проверьте документацию Freezed и Riverpod

---

## ✨ Итого

После выполнения всех шагов у вас будет:

✅ Обновленный main с изменениями из PR
✅ Сгенерированные Freezed файлы для всех моделей
✅ Рабочее приложение, готовое к интеграции с Backend v2
✅ Готовая основа для создания провайдеров и UI интеграции

**Время выполнения:** 15-30 минут (в зависимости от скорости интернета и производительности ПК)

---

**Удачи! 🚀**

**Дата создания:** 2025-12-30
**Версия:** 1.0
**Автор:** Claude Code
